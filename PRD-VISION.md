# domi — 产品愿景与不变量

> 全项目唯一的"不准违反"清单。非重大决策不得修改。
> v1.1 · 2026-09-14 · 修改需走 ADR
> 变更history：v1.0 (2026-08-24) → v1.1 回写自 `docs/adr/003-revert-to-self-built-runtime.md`

---

## 1. 一句话定位

**domi 是一个会积累"人格"的、你能看见它每一步在想什么的本地 Agent 运行时。**

三个支点，缺一个 domi 就变成"又一个 agent 框架"：

- **Soul** —— 跨会话沉淀的人格层，人类可读、可 diff、可否决、可导出分享
- **轨迹** —— 每一步思考、工具调用、权限决策都可见、可回放、可审计
- **本地优先** —— 你的数据在你的机器上，默认拒绝一切权限

**轨迹与评估是一体两面**：事件流可回放 ⟹ 真实会话可以当作测试 fixture 重放。这不是第四个支点，是轨迹支点的第二种消费方式，见 INV-13。

## 2. 目标用户

主：**有 CLI 习惯的开发者**，愿意为可控性接受配置成本，对"黑盒 agent"有戒心。
次：想自建 agent 工作流的技术团队（v1 之后）。

**明确不服务**：非技术用户、需要开箱即用零配置的人、需要多租户/团队协作的组织。

## 3. 成功标准

> v1.1 重写。**项目的真实目标是个人实践与能力证明，不是社区影响力。**
> 这不是降低标准——它把标准从"有多少人用"换成"我能不能讲清楚它为什么这么设计"，后者更难作弊。

| 时间点 | 指标 | 判据 |
|---|---|---|
| M0 | 终端里跑通读→改→测闭环 | demo 录屏 |
| M1 发布 | 自己每天用，且不想切回其他工具 | **连续 5 个工作日**用 domi 完成真实开发任务 |
| M2 发布 | 有一块别人没做出来的东西 | 终端可导航轨迹面板 + 一篇讲透「压缩 × prompt cache 交叉」的文章 |
| M4 发布 | domi 能积累出可分享的人格 | soul 单文件导出，他人可 import 并复现行为差异 |
| M6 / v1.0 | **能讲满 20 分钟的技术决策 ≥ 3 个** | 每个决策都有：候选方案、选择理由、反悔成本、触发重新决策的条件（即 SPEC §2 取舍记录） |

**滞后指标**（不作为目标，只作为观察）：star 数、外部使用者数、非官方插件数。

**反指标（出现即为跑偏）**：
- 功能数量增长但 `docs/spec/*.md` 的"取舍记录"章节是空的 —— 说明在堆功能不在做决策
- 某个里程碑零回写 —— 说明下游在偷偷绕过文档（见 `PROCESS.md` 缺口二）

## 4. 不做什么（v1 范围外，写死）

- 不做多租户、账号体系、计费
- 不做云端会话存储
- 不做可视化流程编辑器
- 不做协商式多 agent（辩论/投票），只做上下文隔离的 sub-agent
- 不做模型微调、不做自研模型
- **不做原生移动端**。唯一例外：**聊天工具桥接**（Telegram，M5+），且只做两种交互——**只读查看轨迹** 与 **远程审批权限请求**。不做移动端发起任务、不做移动端编辑。桥接进程与 daemon 同机，不引入云端中转（守 INV-11）
- 不做 Windows 原生 TUI 优化（WSL 可用即可，v1 后再说）

## 5. 不变量（Invariants）

**违反其中任何一条 = 跑偏。** 与 PRD 其他部分不一致时以本节为准。
**活跃条数上限 12 条**——`PROCESS.md` 缺口三的核心是"不变量的价值在于它少"。v1.1 新增 INV-13 时，把原 INV-02 与 INV-04（同属依赖边界，同一条 CI 规则守）合并，编号不复用，活跃条数仍为 12。

| ID | 不变量 | 守卫方式 |
|---|---|---|
| INV-01 | 会话是 append-only 事件流；事件类型只增不改，任意历史事件永远可解析 | protocol 类型快照测试（CI） |
| INV-02 | **架构边界**：`kernel` 零 IO 依赖（不 import `apps/*`、不 import 具体存储/网络/模型 SDK 实现）；三端零业务逻辑，仅通过 Domi Protocol 与 daemon 通信 | dependency-cruiser + 依赖白名单（CI） |
| INV-03 | 权限默认拒绝；每个权限决策产生一条可审计事件 | 单测默认值断言（CI） |
| ~~INV-04~~ | *已并入 INV-02（v1.1）。编号保留占位，不复用。* | — |
| INV-05 | Tool 是唯一执行原语；Skill 不可执行，只注入上下文 | Capability 类型系统（编译期） |
| INV-06 | MCP / 插件 / 网页的输出是不可信**数据**，不是指令 | guardrail 提示词层 + 注入测试用例 |
| INV-07 | 任意时刻 main 分支可运行 | CI 构建 + 冒烟 |
| INV-08 | 真实 LLM 调用不进 CI 门禁 | CI 配置审查 |
| INV-09 | Soul 是人类可读的 Markdown，可 diff、可手改、可否决 | 格式测试 + 无二进制存储 |
| INV-10 | 每个交付任务反向引用一条 PRD 条目 ID | 任务文件 lint（CI） |
| INV-11 | 用户数据不出本机，除非用户显式配置的模型 API | 出站域名白名单测试 |
| INV-12 | 压缩不销毁原始事件，只产生投影 | 压缩后可重放断言（CI） |
| **INV-13** | **评估以事件流回放为唯一数据源，不新增埋点**；确定性回放层（L1）不得调用真实模型 | 删除 `packages/eval` 后 kernel/store 测试仍全绿；L1 测试在无网络环境下通过（CI） |

## 6. 工程原则：引用优先（v1.1 新增）

回写自 ADR-001。**每个功能进入 SPEC 之前必须先归类**，归错类是本项目最大的时间浪费来源：

| 类别 | 处理方式 | 举例 |
|---|---|---|
| **可直接引用的真库** | 一律不自写。适配层必须薄到"删掉库换一个"只改一个文件 | AI SDK（模型层）· `@modelcontextprotocol/sdk` · Playwright MCP · computer-use MCP · grammY |
| **只抄算法，做精简版** | 读源码理解算法，自己实现，在 SPEC 取舍记录里注明抄自哪里 | shadow git checkpoint（Cline）· ReAct / plan-execute loop |
| **必须自己写** | 没有可引用物，或引用了就没有产出 | 事件流内核 · 轨迹面板 · 压缩与记忆生成 · Soul · 评估体系 |

**硬规则**：第一类功能**不得**出现在成功标准 §3 的判据里——它们让 demo 完整，不构成能力证明。

## 7. PRD 成熟度约定

本项目 PRD 一次性覆盖全部 6 个里程碑，但**不假装远期需求已经想清楚了**。每个里程碑标注成熟度：

| 标记 | 含义 | 修改成本 |
|---|---|---|
| `COMMITTED` | 已想清楚，可直接进入架构设计 | 改动需回写 + ADR |
| `PROVISIONAL` | 方向确定，细节会随实现调整 | 改动只需回写 |
| `SKETCH` | 轮廓级，进入该里程碑前必须重新批准 | 自由修改 |

**再批准门（Re-ratification Gate）**：任何 `PROVISIONAL` / `SKETCH` 里程碑在进入之前，必须重写为 `COMMITTED` 并重新过一次 PM 门禁。**跳过这一步直接开工 = 跑偏。**

这是"一次写全"与"探索性项目"之间的调和方式：全景现在就有，但远期的部分不具备约束力，直到它被重新批准。
