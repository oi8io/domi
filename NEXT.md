在做: M1 代码侧已全部交付（23 个任务 done，331 个测试全绿，10 道守卫）。
下一步: **两件只能你做的事**
        1. M0 走查：真终端跑 `demos/m0-loop.md` 的 13 条（TASK-M0-021 还卡在 review）
        2. M1 dogfooding：连续 5 个工作日用 domi 干真实活——这是 M1 DoD 的判据，代码替不了
        然后另开**独立会话**做 QA（AGENTS.md：不加载实现过程的对话历史，目标是证伪）
卡在: M2 是 PROVISIONAL，进入前必须过再批准门。材料已备好：`docs/prd/M2.md`（COMMITTED 草稿）。
      门上有一个必须由你拍板的决定：**MCP SDK 版本**（对齐 2026-07-28 的 v2 仍是 beta），
      三条路与各自代价写在 `docs/adr/010` 与 M2 草稿里。
      另有两处 M1 的实话见 `docs/qa/M1-reconciliation.md`：cache 命中率与单二进制都没有真实产物。
