{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "窗口里的页面只需要重启能力（更新后）；不开放文件系统、shell 等任何本机能力",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default", "process:allow-restart"]
}
