# M8 SPEC — 工作台

> v0.4 · 2026-09-17（v0.4：SPEC-M8-011 按实现回写；v0.3：取舍-3 / 4 按实现回写）· v0.2 · 作者：架构师环节（v0.2：TUI 并入、项目树、色板存 daemon、会话级授权）
> 上位：`docs/PRD.md` v1.11 §M8 · `docs/prd/M8.md` · `docs/adr/027` · 原型 `docs/ui-redesign/index.html` `docs/ui-redesign/tui.html` · `docs/ui-redesign/HANDOFF.md`

## 1. PRD 覆盖表

| PRD-ID | SPEC 条目 | 落在哪 |
|---|---|---|
| M8-001 token 与主题 | SPEC-M8-001 | `packages/client-core/src/tokens.ts`（唯一来源）、`apps/web/src/globals.css`、`apps/web/src/theme/` |
| M8-002 骨架与导航 | SPEC-M8-002 | `apps/web/src/App.tsx`（壳）、`layout/Sidebar.tsx`、`router.ts` |
| M8-003 项目 | SPEC-M8-003 | `packages/store`（projects 表）、`packages/daemon`（project.*）、`views/ProjectView.tsx` `views/ProjectsView.tsx` |
| M8-004 会话与任务 | SPEC-M8-004 | `packages/runtime/src/session.ts`（kind、沙盒）、`capability/permission.ts`（沙盒范围）、`views/SessionsView.tsx` |
| M8-005 目标驱动的任务 | SPEC-M8-005 | `packages/runtime/src/task-create.ts`、`plan.ts`、`views/TasksView.tsx`（由 `TaskPanel.tsx` 改） |
| M8-006 自动隔离 | SPEC-M8-006 | `packages/runtime/src/isolation.ts`、`session/ChangesBar.tsx`（由 `ChangesPanel.tsx` 改） |
| M8-007 定时任务 | SPEC-M8-007 | `packages/daemon/src/scheduler.ts`、`packages/store`（schedules 表）、`TasksView` 定时分区 |
| M8-008 会话视图 | SPEC-M8-008 | `session/SessionView.tsx`、`Transcript.tsx`、`Trajectory.tsx`、`StatusBar.tsx`、`ConfirmDialog.tsx`；`kernel/metrics.ts` |
| M8-009 运行与未读 | SPEC-M8-009 | `packages/store`（read_marks）、daemon 推送 `sessions.changed` |
| M8-010 Composer | SPEC-M8-010 | `session/Composer.tsx`；daemon `fs.list` `attachment.*` `skill.list` `model.list` |
| M8-011 配置读写 | SPEC-M8-011 | `packages/config/src/write.ts` `secrets.ts`；daemon `config.get/set` |
| M8-012 设置页 | SPEC-M8-012 | `views/settings/*`（吸收 `SoulPanel.tsx` `PluginPanel.tsx`） |
| M8-013 用量 | SPEC-M8-013 | `packages/kernel/src/usage.ts`（纯投影）、daemon `usage.summary` |
| M8-014 TUI 主界面 | SPEC-M8-014 | `apps/tui/src/components/*`、`apps/tui/src/theme.ts` |
| M8-015 TUI 弹层 | SPEC-M8-015 | `apps/tui/src/overlays/*`、`apps/tui/src/keys.ts` |
| M8-016 会话级授权 | SPEC-M8-016 | `packages/capability/src/permission.ts`（grant 表）、两端确认 UI |
| M8-017 TUI 启动上下文 | SPEC-M8-017 | `apps/tui/src/main.tsx`、`packages/cli/src/args.ts`、daemon `project.resolve` |

事件：SCHEMA_VERSION 10 → 11，新增 `session.kind` `project.assign` `schedule.fire`；`user.input` 追加可选 `uploads[]` `files[]` `skills[]`（`attachments` 已被跨会话引用占用，不复用这个名字）；
`plan.decided` 追加可选 `shape` `source`；`permission.source` 增加取值 `session-grant`，`permission` 追加可选 `grant{capability, scope?}`；附 `fixtures/events/legacy-v11.jsonl`。项目、调度、已读位置不是会话事件，进 SQLite 表（迁移 v11）。

## 2. 取舍记录

- **取舍-1 · 会话 / 任务是 kind，不是两张表**。两者的事件流、订阅、渲染完全相同，差别只在工作目录、上下文层与权限范围。
  用 `session.kind` 事件（首条之后立即落）+ sessions 表的 `kind` `project_id` 列。反悔成本低：合并 kind 只需忽略一列。
  重新决策的条件：两者的界面分叉到一半以上的组件不共用。
- **取舍-2 · 项目按路径归属，不按 git 根**。用户可能想把 monorepo 的子目录当项目。归属规则：任务的 cwd 与某个项目路径相同或在其下 → 最深的那个；
  都不是 → 用 cwd 自动建项目（名字 = 目录名）。重新决策的条件：出现大量「同一仓库被拆成多个自动项目」的反馈。
- **取舍-3 · 老会话的迁移**：`cwd` 是 git 仓库（向上找 `.git`，家目录本身是仓库的不算）或含 `AGENT.md` / `AGENTS.md` → `task` + 自动项目（取仓库根）；
  其余（含目录已不存在的）→ `chat`，但 cwd 不改（事件里记录的是历史事实，不能假装它在沙盒里跑过）。
  迁移 SQL 只加列与表（`check-migrations` 不允许 UPDATE）；归类由宿主启动时的 `ProjectService.backfill()` 写 `sessions.kind / project_id`，
  **不往老会话里追加事件**（v0.2 写的「首次打开补一条 session.kind」取消：读一个老会话不该改变它的历史）。
- **取舍-4 · 自由会话的沙盒 = 工作目录**（v0.3 改，回写 PRD v1.11.1）。文件工具的路径收口本来就是「不出工作目录」，把工作目录设成
  `~/.domi/scratch/<会话>` 就得到沙盒，不新开「越界可询问」一档（那会放宽 PRD-M0-003 AC-5）。`shell.exec` 用权限引擎新增的
  `askAlways`：规则 allow 也改成问，deny 不变——只收紧。子 agent 继承 `projectContext:false` 与 `askAlways`。
  反悔条件：用户经常需要在会话里读沙盒外的文件 → 做「把文件带进会话」（附件 / 引用），而不是放宽路径。
- **取舍-5 · 执行形态由模型建议、策略裁决**。`plan.submit` 增加 `shape: single | dag`（模型给）；运行时再按「步骤数 ≥ 3 且存在可并行步骤」校正。
  审阅策略 auto 的阈值：`shape=dag` 或计划里声明的写文件数 > 5 → 要人审。模型可以撒谎说 single，代价是少了并行，不影响安全（写操作仍过权限）。
- **取舍-6 · 自动隔离的判据**：auto 时 git 仓库 且（`git status --porcelain` 非空 或 `shape=dag` 或 由定时触发）→ 隔离。
  理由：工作区干净时直接改 + 步级快照足够安全，且用户能在自己的编辑器里实时看到改动；脏工作区、并行节点、无人值守才是会踩的场景。
- **取舍-7 · 调度器在 daemon 进程内**，不用系统 cron / launchd：跨平台一致、能直接建会话、测试能注入时钟。
  cron 解析自己写（5 段、`*` `,` `-` `/`、月份与星期名；不支持 `L` `W` `#` 与秒），约 150 行，抄 cron-parser 的匹配算法。
  时区用 `Intl.DateTimeFormat` 换算，不引依赖。补跑只补一次（取最近的一个错过时点）。
- **取舍-8 · 已读位置进 daemon**，不放浏览器：用户明确要跨端一致；按「会话 → 最后已读 seq」一张表，不分客户端（单用户）。
  `unread = lastSeq > readSeq 且 最后一条不是用户输入`。当前被任一客户端订阅并处于前台的会话，新事件到达即推进已读。
- **取舍-9 · 配置回写用 `yaml`（eemeli/yaml 2.x）的 Document API**：只改 patch 里的节点，注释与顺序保留。现在读配置用的是 Bun 内置 YAML，它不保留注释；
  自己写行级补丁器要处理流式写法、锚点、多行字符串，出错就是毁用户配置。这是 M8 唯一的后端新依赖，记在 ADR-027。
  反悔条件：Bun 内置 YAML 提供保留注释的写回。
  白名单是**显式列表**（见 SPEC-M8-011），不是黑名单——新加的配置项默认不可从 Web 改。
- **取舍-10 · 附件不进事件流正文**：事件只记 `{id, name, mime, size, sha256}`，内容在 `~/.domi/attachments/<会话>/<id>`；
  回放时附件缺失 → 以「附件已不存在」文本占位，不让回放失败。
- **取舍-11 · 前端路由用 hash**（原型就是这么做的，桌面端 Tauri 也不需要服务端配合），不引路由库。
- **取舍-13 · token 的唯一来源放 client-core**（`tokens.ts`，纯数据，不 import react）。候选：放 `apps/web` 由 TUI 读 CSS（跨 app 依赖，depcruise 不允许）/ 新建 `packages/tokens`（一个只有一张表的包，不值得）。
  Web 的 `globals.css` 仍手写变量（Tailwind 要在构建期看到），由测试断言与 `tokens.ts` 一致。重新决策的条件：出现第三个消费方（桌面端原生窗口）。
- **取舍-14 · 色板存 daemon、深浅模式按设备**（用户拍板 TUI 跟随 Web 的 accent）。`ui.accent` 进 `config.set` 白名单；
  深浅模式是「这个屏幕 / 这个终端」的属性，Web 存 localStorage，TUI 读 `tui.theme` 与 `COLORFGBG`。
- **取舍-15 · 会话级授权是权限引擎里的一张内存表**，由 `permission{source:'session-grant', grant}` 事件重建（重开会话照样生效）。
  范围：非路径能力 = 能力 id；路径能力 = 能力 id + 触发时路径所在目录（前缀匹配）。`shell.exec` 与 MCP / 插件工具不提供（参数语义不可静态限定）。
  子 agent 继承父会话的授权吗？**不继承**——子 agent 只能收窄（M5-001 AC-3），授权是用户对这个会话说的话。
- **取舍-16 · TUI 快捷键在输入框为空时才是单键**；`Ctrl+R` 选它是因为 shell 用户的肌肉记忆（历史搜索），`Ctrl+S` 是 XOFF 不用；
  换行用 `Ctrl+J`（终端里就是 LF，任何终端都能区分）与 `Alt+Enter`，kitty 键盘协议可用时再认 Shift+Enter。
- **取舍-12 · 轨迹时间线在 client-core 投影**：它是「事件 → 区间」的纯函数，与状态栏数字不同，不代表 runtime 的判断，放 client-core 让 TUI 以后能复用。

## 3. 条目

### SPEC-M8-001 · token 与主题

- `apps/web/src/globals.css`：`@import "tailwindcss";`，`:root[data-theme=dark]` / `:root[data-theme=light]` 两套变量（HANDOFF §1 全表 + `--accent-h` `--*-d` `--code-bg` `--shadow`，取原型 `index.html` 的值）；
  `@theme inline` 映射：`--color-bg: var(--bg)` … `--color-tool: var(--tool)`、`--font-sans` `--font-mono`、`--radius-sm/md/lg`；shadcn 别名 `--background: var(--bg)` 等。
- 派生色：`--accent-h: color-mix(in srgb, var(--accent) 80%, white)`；`--accent-d: color-mix(in srgb, var(--accent) 12%, transparent)`（浅色 8%）；`--accent-b` 35% / 30%。
- 色板（`theme/palettes.ts`）：

| id | dark accent | dark emphasis | light accent | light emphasis |
|---|---|---|---|---|
| blue（默认） | `#58a6ff` | `#1f6feb` | `#0969da` | `#0969da` |
| green | `#3fb950` | `#238636` | `#1a7f37` | `#1a7f37` |
| orange | `#f0883e` | `#bd561d` | `#bc4c00` | `#bc4c00` |
| purple | `#a371f7` | `#8957e5` | `#8250df` | `#8250df` |
| pink（新增） | `#db61a2` | `#bf4b8a` | `#bf3989` | `#bf3989` |

  实际取值以 AC-4 的测试为准：不到 4.5:1 的在测试里调暗后回写这张表。
- 色板表与深浅两套 token 定义在 `packages/client-core/src/tokens.ts`（`TOKENS.dark` `TOKENS.light` `PALETTES`），Web 与 TUI 共用（取舍-13）。
- `theme/store.ts`：`$theme: 'system'|'dark'|'light'`（localStorage）、`$accent: PaletteId`（来自 `config.get` 的 `ui.accent`，修改走 `config.set`，未连接时用默认蓝）；`applyTheme()` 写 `data-theme` 与两个变量；
  `index.html` 的 `<head>` 内联 20 行脚本在首帧前读 localStorage 应用，避免闪烁。
- 测试：解析 `globals.css` 与 `HANDOFF.md` 的表比对（AC-1）；`globals.css` 与 `tokens.ts` 比对（AC-6）；WCAG 相对亮度公式算 10 组对比度（AC-4）。

### SPEC-M8-002 · 骨架与导航

- 路由（`router.ts`，`$route` atom 监听 `hashchange`）：

| hash | 视图 |
|---|---|
| `#/` | 空白会话输入页（新对话） |
| `#/s/<sessionId>[/trajectory]` | 会话视图 |
| `#/p/<projectId>` | 项目详情 |
| `#/projects` · `#/sessions` | 全部项目 · 全部会话 |
| `#/tasks[/new][?schedule=1]` · `#/tasks/<runId>` | 任务页 · 新建 · 运行详情 |
| `#/settings/<tab>` | 设置（general / models / messaging / memory / soul / plugins / usage） |

- 组件树：`App`（壳：grid `260px 1fr`）→ `Sidebar`（`Brand` `TopActions` `Section[项目]` `Section[会话]` `Footer`）+ `Main`（按路由切）。
- `Section` 折叠状态存 localStorage（`domi.sidebar.collapsed`）；hover 按钮 `opacity-0 group-hover:opacity-100 transition-opacity duration-150`。
- 项目树（PRD-M8-002 AC-6）：`ProjectNode` = 项目行（文件夹图标、名字、hover 时右侧 ↗ 与笔图标）+ 子列表。
  子列表 `pl-[22px]`，左侧 `border-l border-border2` 作引导线；子项 = 7px 状态点 + 12.5px 标题（单行省略），无 meta 行；
  每个项目最多 5 条（`project.list` 带 `recentTasks[≤5]`，避免 N+1），超出时末尾「查看全部 (N)」（`text-mut`，hover `text-accent`）。
  点行 = 展开 / 收起，展开集合存 localStorage（`domi.sidebar.expanded`）；当前任务所在项目强制展开、项目行 `current` 样式，当前任务行 `accent-d` 底。
- 侧栏会话栏只列 `kind=chat`，最多 10 条（最近活动），超出由「全部会话」图标进入。项目树显示未归档项目，按最近活动排序。
- 连接状态：原型把它放在状态栏 pill；未连接时侧栏品牌行下方另出一行错误（沿用现有 `lastError` 显示），所有需要 daemon 的按钮禁用。

### SPEC-M8-003 · 项目

```ts
// protocol
const ProjectSchema = z.object({
  id: z.string(), name: z.string(), path: z.string(),
  createdAt: z.number().int(), archived: z.boolean(),
  taskCount: z.number().int(), lastActivity: z.number().int().nullable(),
  settings: z.object({
    isolation: z.enum(['auto', 'always', 'never']).default('auto'),
    planReview: z.enum(['auto', 'always', 'never']).default('auto'),
  }),
})
'project.list'    { includeArchived?, recent?: number }  → { projects }   // recent 缺省 5：每个项目带 recentTasks[{id,title,busy,unread}]
'project.create'  { path, name? }                 → { project }     // PATH_NOT_FOUND / NOT_A_DIRECTORY
'project.update'  { id, name?, settings? }        → { project }
'project.archive' { id, archived: boolean }       → { ok: true }
```

- 表：`projects(id TEXT PK, name, path UNIQUE, created_at, archived INTEGER, settings TEXT JSON)`；`sessions` 加 `kind TEXT`、`project_id TEXT NULL`。
- 路径规范化：`realpath` + 去尾斜杠；macOS 大小写不敏感卷上按 `realpath` 的结果比较（不自己 lower-case）。
- 自动登记的路径：目录所在的仓库根（向上找 `.git`），不是仓库就用目录本身。
- 归属（取舍-2）在 `ProjectService.assign(cwd)`（只判断不登记的是 `resolve`）；新任务与迁移共用。落 `project.assign {projectId, auto}` 事件到任务会话里（轨迹里能看到它属于哪个项目、是不是自动建的）。
- 远程安全：`project.create` 只做存在性检查与登记，不读目录内容；信任询问（M7-002）仍在第一个任务开始时发生。

### SPEC-M8-004 · 会话与任务

- `session.create { kind?: 'chat'|'task', projectId?, cwd? }`：`kind` 缺省 → 有 `projectId`、或 `cwd` 在已登记项目里 / 像项目（git 仓库、有规矩文件）→ task，否则 chat。
  这条同时就是 PRD-M8-017 AC-1 的判据（TUI 传 `process.cwd()`）；空参数（旧 Web）→ chat。
  建任务时落 `session.kind{task, cwd}` 与 `project.assign`；建会话时落 `session.kind{chat, cwd: 沙盒}`。长任务运行会话、审阅会话、隔离会话都是任务，子 agent 跟父会话同类。
- chat：cwd = `~/.domi/scratch/<id>`（创建）；`DomiSession` 选项 `projectContext: false` → 跳过 `project.rules` 层、`SkillOverlay`、信任询问；
  `scope` = 沙盒范围（取舍-4）。
- `session.list` 的 `SessionSummary` 追加 `kind` `projectId` `cwd` `busy` `unread` `lastActivity`（全部 optional，旧客户端忽略）。
- `session.toTask { sessionId, projectId, goal }` → 新任务首条输入 = goal + `refs:[{sessionId, fromSeq:1, toSeq:MAX}]` → `{ sessionId }`。
- `session.rename { sessionId, title }` → 只改 sessions 表的 `title` 列（与软删除标记同类的元数据，不是会话内容，不落事件）。
- 回收站：沿用 `includeDeleted` 与 `session.restore`。
- `session.list { kind? }`：侧栏会话栏传 `kind:'chat'`；全部会话页不传。

### SPEC-M8-005 · 目标驱动的任务

- `task.create { projectId, goal, attachments?, schedule? }`：
  1. `ProjectService` 取项目 → 隔离决策（SPEC-M8-006）→ 建 task 会话（`mode: plan`）
  2. 首条输入 = goal；模型在计划模式下读代码、`plan.submit{plan, steps, shape}`
  3. 审阅策略：需要人审 → 走 M7-005 的询问；不需要 → 直接落 `plan.decided{approved:true, source:'policy', shape}`
  4. `shape=dag` → 生成 DAG（M7-005 AC-3），`TaskService.start` 运行，节点会话挂在该任务下；`single` → 同一会话切回 act 继续
  → 返回 `{ sessionId, runId? }`
- `task.list` 统一返回 `{ id, kind: 'session'|'dag', title, projectId, status, startedAt, endedAt?, scheduleId? }`；DAG 运行详情仍用 `task.get`。
- YAML 入口：任务页「高级：粘贴 YAML」折叠区，调用原 `task.start`。
- 小任务不强制先规划：goal 少于 40 字且项目 `planReview != always` 时，不进计划模式，直接提交（避免「改个错别字也要审计划」）。
  （v0.5 改：原写「进了计划模式、第一轮直接给 single 就放过」，实现时发现那样模型仍会先写一份计划，省不下这一轮。）
- 项目的审阅策略只作用于系统替用户开的计划模式（`mode.switch.reason = 新任务先规划`）；用户自己切到计划模式的，仍是 M7-005 的每次都问。
- v0.5 实现说明：`task.create` 返回 `{ sessionId, isolation, planned }`，不带 `runId`（DAG 在计划批准之后才起，经 `plan.decided.runId` 可查）；
  统一的 `task.list` 暂不做（改返回形状会破坏老客户端），任务页把 `session.list{kind:'task'}` 与 `task.list` 合并展示。

### SPEC-M8-006 · 自动隔离与改动条

- `isolation.ts`：`decideIsolation({project, cwd, shape?, trigger}) → {isolate, reason}`，reason ∈ `dirty-worktree | dag | scheduled | policy-always | policy-never | clean | not-a-repo`。
- 隔离时复用 M7 `worktrees.create`；`session.kind` 事件带 `isolation: {isolate, reason}`。
- v0.5 实现说明：`shape` 在 `task.create` 时还不知道，所以 `dag` 这条在计划批准、转运行时判（宿主的 `startTask`）：来源任务已经在单独工作区里的，
  运行就在同一个工作区里跑；否则按项目策略给运行新建一个，决定落在运行会话的 `session.kind` 上。`TaskService.start(yaml, cwd, meta)` 的 meta 原样交给宿主。
- `StatusSnapshot.worktree` 已有；`ChangesBar`：订阅期间每轮结束拉一次 `worktree.diff`（沿用 ChangesPanel 的刷新条件），文件数 > 0 时显示条；
  展开后就是现在的 `ChangesView`（逐文件 diff、丢弃、撤销丢弃、带回三选一）。

### SPEC-M8-007 · 定时任务

```ts
'schedule.list'   {}                                        → { schedules: [{ id, projectId, goal, cron, tz, paused, nextRun, lastRun? }] }
'schedule.create' { projectId, goal, cron, tz? }            → { schedule }   // INVALID_CRON { field, message }
'schedule.update' { id, goal?, cron?, tz?, paused? }        → { schedule }
'schedule.delete' { id }                                    → { ok: true }
'schedule.runNow' { id }                                    → { sessionId }
'schedule.runs'   { id, limit? }                            → { runs: [{ sessionId, due, firedAt, late, skipped?, status }] }
```

- 表：`schedules(id, project_id, goal, cron, tz, paused, created_at, last_due)`、`schedule_runs(schedule_id, due, fired_at, session_id, late, skipped)`。
- `Scheduler`（注入 `now()` 与 `setTimer`）：启动时对每个计划算「上次应触发 ≤ now 且 > last_due」→ 有则补跑一次（`late:true`）；之后按最近的 nextRun 设一个定时器（上限 60s 重算，防系统休眠漂移）。
- 触发 = `task.create{trigger:'schedule'}`；同一计划上一个任务 busy → 记 `skipped:true`，不建会话。
- 界面：任务页「定时」分区（列表 + 新建表单：项目、目标、cron、时区、下一次运行预览）；侧栏「定时任务」→ `#/tasks/new?schedule=1`。
- v0.5 实现说明：cron 与调度器在 `packages/daemon/src/cron.ts` `scheduler.ts`；调度器挂在 core（`Daemon.startScheduler`），触发走与 `task.create` 相同的提交路径，
  忙闲判断用 core 的 busy 表。非法 cron 回 `INVALID_PARAMS`，`data = { reason: 'INVALID_CRON', field }`（不新增错误码：老客户端的枚举不认）。
  追加 `schedule.preview { cron, tz?, count? } → { nextRuns, tz }` 给表单预览；`tz` 缺省取 domid 所在时区。
  `late` = 触发时离应触发时刻超过 60 秒。改时间表 / 从暂停恢复时 `last_due` 置为当前时刻（不补之前的）。

### SPEC-M8-008 · 会话视图

- 组件：`SessionView`（tab 条 + 状态栏 + 内容 + Composer）；`Transcript`（Chat 流）；`Trajectory`（新）；`StatusBar`（pill）；`ConfirmCard`（`ConfirmDialog.tsx` 改名重写，导出名保留 `ConfirmDialog` 一个版本以免断测试）。
- `MetricsSnapshot` 追加（kernel `aggregate` 计算，client-core 透传）：`turns`（user.input 数）、`steps`（模型请求数）、`tokPerSec`（最近一轮输出 token ÷ 模型流式耗时）、`cacheHitPercent`（cacheRead ÷ input 总量）。
- 思考耗时：`reason` 条目追加 `ms`（store 投影时用相邻事件 ts 差，属于展示投影）；工具耗时同理用 call / result 的 ts 差。
- Trajectory：`client-core` 新增 `trajectory(items, envelopes)` → `{turns:[{index, rows:[{tag, text, seq}]}], timeline:{input[], model[], tools[]}}`；
  过滤 Duration（时间线开）/ Turns（按轮折叠）/ Calls（只看工具行）；搜索对 text 做包含匹配。
- 消息 hover 操作：`group-hover` 显示「分支」「引用这一轮」（沿用 `turnRanges` 与 `BranchButton` 逻辑）。会话标题行（tab 条左侧）：标题可点改名、菜单：转为任务 / 删除（两步）。
- 确认卡默认焦点在「拒绝」（autoFocus，沿用 biome-ignore 注释）。

### SPEC-M8-009 · 运行与未读

- 表 `read_marks(session_id PK, seq)`；`session.read { sessionId, seq }`（客户端在会话视图可见且滚到底时发，节流 1s）。
- 通知 `sessions.changed { sessionIds[] }`：会话创建、删除、busy 变化、新事件（节流 500ms 合并）时推给所有连接；客户端收到后重新 `session.list`。
- `busy` 来自 runtime host 的活动会话表（已有 `session.busy` 事件的来源）。

### SPEC-M8-010 · Composer

- `fs.list { sessionId, query?, limit? }` → `{ files[], truncated }`：会话 cwd 下的文件清单（M7 `file-list`），模糊匹配；只读，不经权限询问（只返回路径，不返回内容）。
- 附件：`attachment.put { sessionId, name, mime, dataBase64 }` → `{ id, size, sha256 }`（WebSocket 帧上限内；> 上限 → `TOO_LARGE`），
  `session.submit` 追加 `uploads: [id]` `skills: [name]` `files: [path]`；`files` 作为引用行进首条输入（「用户引用了这些文件：…」），内容由模型自己去读（仍过权限）。
- 模型能力：`capability.vision` 为 false 时图片附件 → 提交前拒绝 `UNSUPPORTED_ATTACHMENT`，界面提示换模型。
- `skill.list {}` → `{ skills: [{name, description, source}] }`；选中的 Skill 在这一轮以 `skill.load` 同样的内容注入（记 `user.input.skills`，buildContext 渲染）。
- `model.list {}` → `{ models: [{provider, name, label, capabilities}], current }`：来自 SPEC-M8-011 的 providers 配置 + 内置价目表里的名字。
- 键盘：Enter 发送（IME 组合中不发送，`isComposing`）；`@` / `/` 在行首或空格后触发弹层。
- v0.5 实现说明：`TOO_LARGE` / `UNSUPPORTED_ATTACHMENT` 不新增错误码，都是 `INVALID_PARAMS` + `data.reason`（另有 NOT_FOUND / INVALID）；
  `skill.list` 可带 `sessionId`（带上才有该会话仓库里的项目技能）；`model.list` 每项是 `{provider, name, vision, toolCall}`，
  模型名来源多了「这家长期稳定的 API 名」（目前只有 deepseek 两个，写在 model/factory.ts）；
  附件内容经 kernel 的 `InputResolver` 端口读（kernel 不碰 IO），`ModelMessage` 的 user 追加 `images[]`；
  文本附件最多放 20 万字符进上下文；图片按每张约 1600 token 估算上下文占用。

### SPEC-M8-011 · 配置读写与凭据

- 配置追加 `providers`（v0.4 按实现改）：文件里 `providers.<名字>.base_url` / `api_key` / `models`，名字取 `anthropic` `openai` `deepseek` `openai-compatible`
  （只有一个兼容网关，原型也只画了一个）；`model.provider/name` 仍是默认模型，旧的 `model.api_key/base_url` 继续认。
  会话换到别家模型时用那一家的凭据（之前是沿用默认那一家的）。DeepSeek 没配 base_url 时用官方地址（写在 model/factory.ts，provider 名只出现在那里）。
- `context` 追加 `keepTurns`（默认 2，与原先的硬编码一致）、`compactAt`（百分比，默认 70）；「结构化清理」就是 `context.strategy`（full / clean / compact），不另加开关（PRD v1.11.2）。
- 白名单（`config/write.ts` 的 `WRITABLE_KEYS`）：`model.provider` `model.name` `providers.<四家>.base_url|api_key` `providers.openai-compatible.models`
  `memory.extractEvery` `memory.soul` `context.strategy|keepTurns|compactAt` `plugins.disabled` `verify.enabled` `budget.*` `ui.accent` `tui.theme`。
- `secrets.yaml`：键路径同 config；写入先写临时文件（0600）再 rename；读取时文件权限比 0600 宽 → doctor 标红但仍读取。
- `config.get` → `{ values, secrets: {<家>: {set, masked?, source?}}, paths, secretsTooOpen, writable }`；
  `config.set { patch }` → `{ ok, restartRequired }`：`memory.*`（MemoryService 启动时读）与 `plugins.*`（插件启动时加载）列为要重启。
- 校验：补丁先落到临时目录里的一份副本上，用 `loadConfig(env: {})` 走一遍，过了才写真文件；空字符串的 key = 删除。
- 热加载：宿主重新 `loadConfig`，新会话用新配置；已开的会话 `DomiSession.reconfigure()`（换配置、按新凭据重建模型实例），正在跑的一轮不受影响。
  `load.ts` 与 `secrets.ts` 共用的 `ConfigParseError` 挪到 `errors.ts`（depcruise 不许循环依赖）。

### SPEC-M8-012 · 设置页

- `views/settings/SettingsView.tsx`（左侧 160px tab）+ 每个 tab 一个文件；表单用受控组件，失焦或点「保存」时 `config.set`。
- Soul tab 吸收 `SoulPanel.tsx`：Soul 文本可编辑（新 RPC `soul.write {text}`，写文件并落一条与手改等价的记录，沿用 M4-003 的「人改过的行不再动」规则）；
  导出 / 导入走已有的 `domi soul export/import` 实现，新 RPC `soul.export` / `soul.import {text}`（导入先出 diff 走审阅）。
- 插件 tab 吸收 `PluginPanel.tsx`：开关写 `plugins.disabled`；`plugin.list` 追加 `enabled`。
- 通讯工具 tab：两张禁用卡片，文案「即将支持」。

### SPEC-M8-013 · 用量

- `kernel/usage.ts`：`summarize(sessions: Iterable<{events, model}>, range)` → `{ totals, byModel[] }`；daemon 按 `updated_at` 先筛会话再读事件，结果按月缓存（当月不缓存）。
- 柱状图：手写 SVG，横向条形（模型名长），颜色用 `--accent`，数值标签在条右侧。

### SPEC-M8-014 · TUI 主界面

- 布局（自上而下）：`ContextBar`（`项目 › 标题` · `N turns`）→ `Transcript` → 内嵌 `PermissionBox` / 表单 → `Spinner` → `InputLine` → `StatusBar` → `KeyHints`。
- 行前缀与颜色表（`theme.ts` 的 `LINE_STYLE`）：user `›` ok · reason `·` info 前缀 + mut 正文 · tool-call `⚙` tool（右侧 meta `状态 · 耗时` mut2）·
  tool-result `←` ok 时 ink2 / 失败 bad · assistant `✓` accent，正文前加 `domi: ` · context `✂` accent（meta：`68K / 128K tokens · 83% cache hit`）· task `▸` info · error `✗` bad。
- 颜色解析：`resolveColor(token)` → truecolor 时 hex；否则按表映射到 16 色（accent→blue、ok→green、warn→yellow、bad→red、info→magenta、tool→yellowBright、mut→gray）；
  truecolor 判定 `COLORTERM ∈ {truecolor, 24bit}`；`NO_COLOR` 时 Ink 的 `color` 一律不传。
- 深浅：`tui.theme`（config，默认 auto）；auto 解析 `COLORFGBG` 的背景位（`7` `15` 视为浅色），解析不到 → 深色。
- `PermissionBox`：边框 warn；标题 `🔑 权限请求：<动作>`；明细一行 `<能力> · <路径>`（路径 tool 色）+ 说明；按键 chip：`y 允许` `n 拒绝`（高亮） `a 本会话始终允许`（shell.exec 时不显示）；
  `Enter` = n。表单询问（计划审批 / elicitation）用同一个框，字段逐行编辑（`Tab` 切换）。
- `InputLine`：Ink 的 `useInput` 读 `key.return`；`\n`（Ctrl+J）或 `meta+return` 插入换行；启动时发 kitty 协议查询 `CSI ? u`，有应答再开启 `CSI > 1 u` 并认 Shift+Enter，退出时关闭。
- 金样：`golden.spec.tsx` 四种宽度重录；状态栏 40 列折行测试保留。

### SPEC-M8-015 · TUI 弹层

- `keys.ts`：`routeKey(input, key, {inputEmpty, overlay})` → 动作，纯函数可测；单键仅在 `inputEmpty && overlay===null` 时生效。
- `Overlay` 通用组件：标题、搜索行（直接打字即搜索）、分组列表、底部按键提示；宽度 `min(80, 列数-4)`，居中；`↑↓` / `Ctrl+N` `Ctrl+P`（弹层内）移动。
- 项目弹层数据 `project.list`；会话弹层 `session.list`（分组、状态点字符 `●`，运行中每 500ms 在 `●` `○` 之间切换代替脉冲）；
  任务弹层 `task.list` + `schedule.list`；新建表单三字段，cron 失焦时调 `schedule.create` 的校验（`dryRun:true`）显示错误段。
- `/` 补全：从 `commands.ts` 的命令表生成；`@` 补全：`fs.list`，选中后插入 `@相对路径`，提交时解析为 `files[]`。
- 测试：`test/render.tsx` 增加 `press(keys)`（假 stdin 写入序列）驱动按键。

### SPEC-M8-016 · 会话级授权

- `session.answer` 追加可选 `grant: boolean`（v0.5 按实现改：原写 `decision` 三选一，和已有的 `allowed` 会出现矛盾组合）；只在 `allowed` 且询问 `grantable` 时生效。
- 引擎顺序：模式范围（计划模式）→ 会话范围（沙盒）→ **会话授权** → 配置规则 → 默认。会话授权只能把 `ask` 变 `allow`，不能越过 `deny`。
- 事件：`permission{decision:'allow', source:'session-grant', grant:{capability, scope}}`；重开会话时 `DomiSession` 从事件重建授权表。
- 自由会话沙盒外的 ask 可授权，scope 为具体目录；`shell.exec` / `mcp.*` / 插件工具的询问不返回该选项（询问事件带 `grantable:false`）。

### SPEC-M8-017 · TUI 启动上下文

- `project.resolve { cwd }` → `{ project?: Project, isProjectLike: boolean }`（不登记，只判断）；`session.create{kind:'task', cwd}` 负责登记。
- `domi`：`isProjectLike` 或命中项目 → task；否则 chat（判据在 daemon 的 `session.create`，TUI 只传 cwd）。`--chat` → chat；
  `-p <名或路径>`（长写法 `--in`）→ 名字精确匹配 → 路径 → 模糊候选（编辑距离 ≤ 3，最多 5 个）报错。
- 只改 `args.ts` 的参数表与 `main.tsx` 的建会话分支；`-c` / 打开已有会话不改 kind。

## 4. 前端文件布局（替换关系）

| 现有 | 去向 |
|---|---|
| `App.tsx` | 壳 + 路由；`SessionView` `SessionTools` `ModeToggle` 移到 `session/` 并按原型重写 |
| `Transcript.tsx` | 原地重写样式；`groupRows` `turnRanges` 保留 |
| `StatusBar.tsx` | 原地改成 pill |
| `ConfirmDialog.tsx` | 原地改成内嵌卡片 |
| `TaskPanel.tsx` | 改为 `views/TasksView.tsx`（git mv） |
| `SoulPanel.tsx` · `PluginPanel.tsx` | 改为 `views/settings/SoulTab.tsx` · `PluginsTab.tsx`（git mv） |
| `ChangesPanel.tsx` | 改为 `session/ChangesBar.tsx`（git mv） |
| `PendingRefs.tsx` | 并入 Composer |
| `styles.css` | 删除，由 `globals.css` 取代 |
| TUI `components/Transcript.tsx` `StatusBar.tsx` `ConfirmDialog.tsx` `Prompt.tsx` | 原地改（ConfirmDialog → 内嵌 PermissionBox，文件名保留）；新增 `ContextBar` `KeyHints` `overlays/` |

## 5. 不变量影响分析

| INV | 触及点 | 守卫 |
|---|---|---|
| INV-01 | 新事件、`user.input` 追加字段 | `guard:append-only` + legacy-v11 fixture |
| INV-03 | 会话沙盒、Web 不能改权限类配置、确认卡与 TUI 权限框默认拒绝、会话级授权不越过 deny | runtime/session-kind.spec、config/write.spec（造违规：patch 带 `hooks` 必须被拒）、render 测试 |
| INV-04 | 项目归属、未读、调度、隔离都在 daemon | `guard:deps`（apps/web 不许 import runtime / store） |
| INV-11 | Vite dev server 仍只监听 127.0.0.1 | 现有 |
| INV-13 | 新指标与用量只从事件投影 | kernel 单测；`guard:purity` |
| NFR-06 | scratch / attachments / secrets 都在 `~/.domi/` | `guard:writes` 追加三个目录 |

## 6. 明确留白

- 通讯工具的配置结构（等参考 openclaw 后再定）。
- 附件的文本抽取（PDF、docx）：本轮不支持的类型如实拒绝。
- 任务的并行上限、调度的并发上限：先各 1（同一计划不并发），实际用起来再定。
- 暗色 / 浅色之外的高对比主题。
