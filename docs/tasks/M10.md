# M10 任务 — 会话体验与运行控制

> status 取值：todo | doing | blocked | review | done | dropped
> 上位：`docs/prd/M10.md` · `docs/PRD.md` v1.13 §M10 · `docs/spec/M10.md` · 复核 `docs/qa/M10-prd-review.md`
> 用户规矩：**先推进功能，测试验证类最后统一查漏补缺**——验证待办归 TASK-M10-007；守卫与纯函数单测随任务写（守卫先造违规证明会红）。
> 顺序：000 → 001 → 002（回归）→ 003 → 004 → 005 → 006 收口 → 007 最后。
> 000 是**前置正式化**：工作区 `loop.ts` 的 20→100 改动已存在（M10-003 默认值就是 100），先把它的测试与回写坐实，再往上盖。

**M10 DoD**：见 `docs/PRD.md` §M10 末尾。→ 只有用户能做。

### TASK-M10-000 · 前置正式化：loop 默认值 100 与 M0 回写坐实
- status: done
- prd: PRD-M10-003（AC-1）、PRD-M0-002（AC-2 回写）
- spec: SPEC-M10 取舍-3
- 说明：工作区 `kernel/src/loop.ts` 的 `maxToolCalls: 20 → 100` 未提交；M10-003 默认值就是 100。本任务把它变成有测试保护的正式状态，M0-002 AC-2 的「20 次」回写在 `docs/PRD.md` v1.13 已划掉改引用 `loop.maxToolCalls`。
- 测试:
  - [x] 确认 `kernel/test/loop.spec.ts` 的 `20-tool-loop` fixture：按注入 `limits: { maxToolCalls: 20 }` 验证行为（不依赖默认值）→ 若依赖默认值则改为注入（先红后绿）
  - [x] 新增一条断言：`DEFAULT_LIMITS.maxToolCalls === 100`（把工作区改动变成被测试覆盖的事实，先红后绿）
- 实现:
  - [x] 提交工作区 `packages/kernel/src/loop.ts`（20→100，含上面测试）
  - [x] commit message 带 TASK-M10-000
- 笔记（2026-09-19）：fixture 实况——`20-tool-loop` **原本没有注入 limits**，直接依赖 DEFAULT_LIMITS 的 20；工作区改 100 后它会红（HANDOFF 说的「让 AC-2 测试红」就是这个）。已改为注入 `maxToolCalls: 20`（断言不变：第 20 次后停）+ 新增 `DEFAULT_LIMITS.maxToolCalls === 100` 断言。commit `1cc44297`。

### TASK-M10-001 · 会话标题接入运行时
- status: done
- prd: PRD-M10-001
- spec: SPEC-M10 取舍-1、2
- 前置: 无
- 测试:
  - [x] `runtime/test/session-title.spec.ts`（新）：注入模型替身；第一轮 assistant 落盘后 `maybeAutoTitle` 触发一次并调用 `generateTitle`；第二轮不重复触发；失败不阻塞 submit 返回
  - [x] `store/test/sessions.spec.ts`（扩展）：`list()` 空标题会话返回 `first_input`（首条 user.input 前 40 字）；有标题不覆盖；无 user.input 返回空
  - [x] `daemon/test/runtime-host.spec.ts`（扩展）：`session.list` 空标题 → fallback；非空 → 原样
  - [x] `apps/web/test/`（扩展）：`titleOf` 不再回退 id（空标题显示 fallback 值）——Web 无独立 titleOf 测试，行为由 daemon 投影 + store 派生列测试覆盖
- 实现:
  - [x] `runtime/src/session.ts`：`submit()` 成功路径末尾 `void this.maybeAutoTitle()`；判据 = title 空 && 已有 model.delta 事件 && 未试过（实例布尔）
  - [x] `store/src/sessions.ts`：`list()` SQL 加 `first_input` 派生列（`json_extract(payload,'$.text')` 前 40 字，走 `idx_events_type`）；`SessionSummary` / `RawRow` 加字段
  - [x] `daemon/src/runtime-host.ts` `list()`：`title` 空 → 用 `firstInput` 投影
  - [x] `apps/web/src/layout/data.ts`：`titleOf` 改为返回 `title`
- 笔记（2026-09-19）：
  1. **判据修正**：SPEC 写「已有 assistant 事件」，但协议里**没有 `assistant` 事件类型**——模型回复是 `model.request` + `model.delta`。判据改用 `model.delta`。
  2. **StubProvider 加 `titleScript`**：自动标题插队暴露了两个问题——(a) 标题生成按轮次消费替身剧本，多轮测试 turn 错位（verify-gate AC-3 三态、plan-mode 全乱）；(b) calls 数组记录标题请求，`calls.at(-1)` / `calls.length` 断言被扰动。解决：替身识别标题请求（提示词特征 `起一个不超过 20 字的标题`），有 `titleScript` 则独立消费（不占对话轮次），否则返回固定 `{"title":"自动标题"}`；导出 `isTitleRequest` 供测试过滤 calls。title-gen / session-title 显式传 titleScript 断言标题内容与降级；verify-gate / plan-mode / project-rules / model-switch 过滤后再断言。
  3. **macOS 平台适配**：projects.spec.ts 的路径断言在 Linux VM 绿、Mac 红——`/var` 是 `/private/var` 的符号链接，生产代码 realpath 规范化，测试改用 `realpathSync` 同口径。
  4. 受影响文件：title-gen.spec.ts（titleScript）、runtime-host.spec.ts（refs 断言 at(-1)→some()）、verify-gate.spec.ts、plan-mode.spec.ts、project-rules.spec.ts、model-switch.spec.ts。runtime+daemon+store 全量 334 测试绿。commit 见 `git log --grep TASK-M10-001`。

### TASK-M10-002 · 默认模型互斥回归（无新代码）
- status: done
- prd: PRD-M10-002
- spec: SPEC-M10 取舍-6
- 测试:
  - [x] 核对 `config/test/providers.spec.ts`：有「设某模型为默认后旧默认失效」断言；缺则补——缺，补 AC-1 回归测试
  - [x] 核对：「默认模型所在 provider 不可停用 / 删除（`DEFAULT_PROVIDER`）」断言；缺则补——已有（AC-5 测试）
- 实现:
  - [x] 无实现改动（核对中发现缺口，补测试即可）
- 笔记（2026-09-19）：AC-2 断言本来就有（AC-5 测试，reason=DEFAULT_PROVIDER）；缺 AC-1。新测试：anthropic 默认时删它被拒 → 写 model.provider/name 切到 deepseek → deepseek 接管保护（删它被拒）→ anthropic 保护解除（可停用）。首版多断言了一条 `providerConnection(cfg,'deepseek').models` 含模型名，deepseek 模板不带默认模型清单，删掉改为断言 model.name。commit `0eb4aa9`。

### TASK-M10-003 · 运行时护栏可配置
- status: done
- prd: PRD-M10-003
- spec: SPEC-M10 取舍-3
- 前置: TASK-M10-000
- 测试:
  - [x] `config/test/schema.spec.ts`（新）：`loop` 节默认 100 / 3 / 600_000；非法值（非整数 / 0 / 负数 / 超上限）被拒——注意 `ConfigSchema.parse({})` 需要 model，测试用 minimal 包装
  - [x] `config/test/write.spec.ts`（扩展）：`loop.*` 三键白名单放行；非法值 `ConfigWriteError` 且文件不动
  - [x] `runtime/test/loop-limits.spec.ts`（新）：`config.loop.maxToolCalls=2` 时一轮在第 2 次工具调用后停（error{recoverable, counters.toolCalls=2}）
  - [x] `packages/cli/test/cli.spec.ts`（扩展）：doctor 传 loop 显示生效限制；没传不查
- 实现:
  - [x] `config/src/schema.ts`：`loop` 节（`maxToolCalls` / `maxArgParseRetries` / `maxWallClockMs`，值域校验）
  - [x] `config/src/write.ts`：白名单加 3 键；`readSettings` values 加 3 项
  - [x] `runtime/src/session.ts`：`runTurn` deps 传 `limits`（从 config 读）
  - [x] `apps/web/src/views/settings/RuntimeTab.tsx`（新）+ `SettingsView.tsx` 加 tab：三个数字输入、保存走 `config.set`（router SETTINGS_TABS 加 'runtime'，views.spec.tsx 7→8 个 tab）
  - [x] `packages/cli/src/doctor.ts`：新增一条 loop 限制 Finding（`cli.doctor.loop*` i18n）
- 笔记（2026-09-19）：
  1. **typecheck 欠账**：M10-001 提交时只跑了 bun test 没跑 tsc，`pnpm typecheck` 揪出 5 个类型错误。根因是 titleScript 被声明成单轮 `StubTurn`，但语义是多轮剧本——改为 `StubTurn[]`（数组按标题请求轮次消费，越界回退末轮，元素可以是事件数组或函数）。连带修 model-switch.spec 的 bodies 过滤（JSON.stringify(b.messages) → JSON.stringify(b)，bodies 类型没有 messages）。
  2. **loop-limits 断言教训**：kernel stop 时会为 pending（没来得及跑的调用）配结果，事件流里的 `tool.call` 数会**多于**实际执行数。精确断言用 error.counters.toolCalls === limits（kernel 计数），不断言事件条数。
  3. runtime 测试补在 `loop-limits.spec.ts`（TASK 清单原写「runtime/test/ 扩展」，新建文件更清晰）。
  4. commit：`b78f3f3`（003）+ `01fa4df`（001 类型欠账）。


### TASK-M10-004 · TUI 设置：语言切换
- status: done
- prd: PRD-M10-004
- spec: SPEC-M10 取舍-4
- 测试:
  - [x] `apps/tui/test/commands.spec.ts`（扩展）：`/settings` 解析 + COMMANDS 含它 + `completeSlash('/set')` 可见（AC-3）；`languageItems` 三项与 Web 一致、当前值点亮
  - [x] `apps/tui/test/overlays.spec.tsx`（扩展）：设置层渲染三项，回车选择调 `setSettings({'ui.locale': v})`，提示「重启后生效」
  - [x] `apps/tui/test/golden.spec.tsx`（新 describe）：en 场景 80 列快照 `scene-settings.en.txt`
- 实现:
  - [x] `apps/tui/src/commands.ts`：COMMANDS 加 `/settings`；`parseSlash` 返回 `{ kind: 'settings' }`
  - [x] `apps/tui/src/keys.ts`：OverlayId 加 'settings'
  - [x] `apps/tui/src/main.tsx`：`case 'settings': setOverlay({ id: 'settings' })`
  - [x] `apps/tui/src/overlays/Overlays.tsx`：`languageItems`（auto/zh/en，当前值点亮）+ `SettingsOverlay`（选择 → client.setSettings → notice「重启后生效」）+ 分派
  - [x] i18n：`tui.cmd.settings` / `tui.settings.title` / `tui.settings.appliesOnRestart`
- 笔记（2026-09-19）：
  1. SPEC 取舍-4 定的方案落地：`/settings` 命令 + 选择层，写 `ui.locale`，提示「下次启动生效」（TUI locale 启动时定死，不做热切换）。`/lang` 命令实际不存在于 TUI 源码（M9 描述残留），无冲突。
  2. `web.settings.langZh` 在 en 界面下仍是「简体中文」（语言名保持母语显示，M9 既有行为），golden 断言按此写。
  3. 修了既有断言：overlays.spec 补全候选 `/s` 现在含 `/settings`。commit 见 `git log --grep TASK-M10-004`。


### TASK-M10-005 · 思考过程默认折叠
- status: done
- prd: PRD-M10-005
- spec: SPEC-M10 取舍-5（Web 原生 `<details>` 默认不 open；TUI `e` 键切换——**非 Enter**：Enter 是发送键）
- 测试:
  - [x] `client-core` 截断函数单测（`store.spec.ts` 新 describe，复用 `ARG_SUMMARY_LIMIT` 口径：短文本原样 / 80 字截断加省略号）
  - [x] Web 组件测试（`session-view.spec.tsx`）：reason 默认折叠为单行「思考 · 摘要」，`details` 不带 `open`，全文仍在 body；非 reason 条目不受影响（既有断言照旧）
  - [x] TUI 测试（`viewport.spec.tsx` 新 describe + `keys.ts` 纯函数）：reason 默认折叠 / `reasonsExpanded=true` 展开；`isReasonToggle` 输入框空 + 无修饰键才生效；与滚动键（PgUp/PgDn/Ctrl+Home/End）不冲突
- 实现:
  - [x] `client-core/src/store.ts`：导出 `summarizeReason`（与 `summarizeArgs` 同一 `ARG_SUMMARY_LIMIT` 截断口径，渲染层不二次截断）
  - [x] `apps/web/src/Transcript.tsx`：reason 改为 `<details>` 默认不 open，折叠态 summary 显示「▸ 思考 · 摘要」（`summarizeReason`）
  - [x] `apps/tui/src/components/Transcript.tsx`：`Line` 加 `collapsed` prop；`Transcript`/`ClassicTranscript`/`itemLines`/`transcriptLines`/`useTranscriptLines` 贯穿 `reasonsExpanded`（默认 false）；`lineCache` 缓存键加 `collapsed`
  - [x] `apps/tui/src/App.tsx`：`reasonsExpanded` prop 贯穿（含 `FullscreenLayout` → `useTranscriptLines`）
  - [x] `apps/tui/src/main.tsx`：`useState` 持折叠态，输入框空时 `e` 切换；判定抽成 `keys.ts` 的 `isReasonToggle` 纯函数
  - [x] golden 快照重生成：`scene-40/60/80/200.txt` + `scene-80.en.txt` 的 reason 行改为折叠形态（`· 思考 · 先看看文件内容` / `· Thinking · …`），已逐一读 diff 确认仅 reason 行变化
- 笔记：方案按 SPEC 取舍-5 落地。要点：(1) Web 用原生 `<details>` 语义，折叠是展示状态不进 store；折叠态 summary 是「▸ 思考 · 摘要」（`group-open/thought` 控制 ▸/▾），全文仍在 body，点击由浏览器处理。(2) TUI `e` 键不占滚动键（滚动键是 PgUp/PgDn/Ctrl+Home/End，见 `render/viewport.ts` 的 `scrollKey`），输入框非空时 `e` 正常打字。(3) 截断口径统一在 client-core 的 `summarizeReason`（80 字 + …），两个渲染层都只调用不再截断。(4) 教训：TUI 折叠行的 CJK 双宽宽度 ≈ 171，测试用 160 宽会折行导致断言失败，折叠断言用 200 宽、展开断言用 400 宽；`pnpm typecheck` 抓出 App.tsx 解构加了 `reasonsExpanded` 但类型注解漏加——提交前 typecheck 必须全绿。

### TASK-M10-006 · 收口：AC 覆盖、协议快照与全量验证
- status: done
- prd: PRD-M10-001…005
- spec: SPEC-M10 §5
- 前置: TASK-M10-001…005
- 实现:
  - [x] `check-ac-coverage` 的 `ACTIVE` 加 M10（`/^PRD-M(?:[01]|[789]|10)-/`）+ 注释与 OK 文案同步；实测 224 条 AC 全被测试点名（M10-002 的 AC 走 providers.spec.ts）
  - [x] 协议文档 / API 快照：`gen-protocol-docs.ts --check` 与 `gen-api-snapshot.ts --check` 均 OK —— `config.get` 的 values/writable 是宽松 schema（`z.record(z.string(), z.unknown())` / `z.array(z.string())`），loop.* 是运行值，rpc.ts 未变，快照无需重生成
  - [x] `pnpm check` 全绿（typecheck + lint + guard + 全部测试 + L1 回放）；首跑在 lint 阶段抓出 12 个 biome 格式错误（M10-001 的 store 文件与 003/004/005 的部分文件提交时未过 lint），`biome check --write` 修复后相关包 501 测 + typecheck 回归全绿
- 笔记：M10-006 是本里程碑的收口门。实际核查发现摘要里「协议快照需重生成」的预期不成立：`config.get` 的返回类型是宽松 record，`loop.*` 三键只体现在运行值里，协议 schema 与快照文件零 diff；AC 覆盖是唯一有代码改动的项（`scripts/check-ac-coverage.ts` 的 `ACTIVE` 正则 + 文案）。全量 `pnpm check` 最终通过（第一轮 lint 阶段挂过，见上）。

### TASK-M10-007 · 验证补齐（最后做）
- status: todo
- prd: PRD-M10-001…005
- spec: SPEC-M10 §5.2
- 前置: TASK-M10-006
- 实现:
  - [ ] `check-ac-coverage --report` 逐个里程碑过 AC：M10 全部有测试；顺带检查 M4 / M5 / M6 的验证缺口（TASK-M4-009 / M5-008 / M6-008 仍 todo，本任务不越界做，只列缺口）
  - [ ] 用户侧 DoD 清单整理（Web 侧栏标题、loop 生效、TUI 切语言、思考折叠）→ 等用户走查
- 笔记：待写
