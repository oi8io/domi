# M5 任务 — 会干活

> status 取值：todo | doing | blocked | review | done | dropped
> 上位：`docs/prd/M5.md`（再批准门）· `docs/PRD.md` v1.6 §M5 · `docs/adr/020` · `docs/adr/021`
> 用户规矩：**先推进功能，测试验证类最后统一查漏补缺**——「验证待办」都归到 TASK-M5-008。

**M5 DoD**：一个 30 分钟、含 5+ 节点的真实任务跑通；中途 kill daemon 后能恢复并完成；完成时收到通知。
→ 只有用户能做（真实模型、真实时长），放在最后。

### TASK-M5-001 · Sub-agent
- status: todo
- prd: PRD-M5-001
- 实现:
  - [ ] protocol：`task.spawn` 等 task.* 事件（SCHEMA_VERSION 7 → 8，legacy fixture）
  - [ ] store：`sessions.spawned_by` 列；子会话不进默认列表
  - [ ] runtime：`task.spawn` 工具；父范围权限；嵌套上限；父上下文只拿结论
  - [ ] trace：task.spawn 节点下嵌套子会话轨迹；Web 工具行带子会话链接

### TASK-M5-002 · DAG 编排
- status: todo
- prd: PRD-M5-002
- 实现:
  - [ ] packages/orchestrator：YAML + zod、环检测、`runState`、`nextNodes`
  - [ ] runtime：四种节点执行器；运行会话；失败节点单独重试
  - [ ] 协议 `task.start` / `task.list` / `task.get` / `task.retry`；CLI `domi task run|list|status|retry`；client-core 投影 task.*

### TASK-M5-003 · 断点恢复
- status: todo
- prd: PRD-M5-003
- 实现:
  - [ ] domid 启动时恢复未结束的运行，落 `task.resume`

### TASK-M5-004 · 长任务通知
- status: todo
- prd: PRD-M5-004
- 实现:
  - [ ] packages/notify：系统通知 + webhook；payload 脱敏；失败不抛
  - [ ] daemon：运行完成 / 失败 / 待确认时触发

### TASK-M5-005 · 桌面端
- status: todo
- prd: PRD-M5-005
- 实现:
  - [ ] apps/desktop：Tauri 2 配置、Rust 入口、更新检查
  - [ ] `scripts/check-desktop-size.ts` 接进 guard（先造违规证明会红）；depcruise 覆盖 apps/desktop

### TASK-M5-006 · 协议里的审批渠道
- status: todo
- prd: PRD-M5-007
- 实现:
  - [ ] `session.answer` 带 `channel`；permission 事件带 `channel`；审计方法 `audit.record`

### TASK-M5-007 · Telegram 桥接
- status: todo
- prd: PRD-M5-007
- 实现:
  - [ ] apps/bridge-telegram：grammY 1.46.0；配对码与白名单；推送；按钮审批；退避
  - [ ] `formatBridgeMessage` + `scripts/check-bridge-payload.ts`（先造违规证明会红）
  - [ ] CLI `domi bridge pair|telegram`；depcruise 覆盖 apps/bridge-telegram

### TASK-M5-008 · 验证补齐（最后做）
- status: todo
- prd: PRD-M5-001, PRD-M5-002, PRD-M5-003, PRD-M5-004, PRD-M5-005, PRD-M5-007
- 验证待办:
  - [ ] orchestrator/subagent.spec · dag.spec · resume.spec；trace/nested.spec
  - [ ] bench/kill-fuzz.ts（10 个随机时点 kill -9）
  - [ ] notify/*.spec（含 webhook 500 时任务照常完成）
  - [ ] bridge/telegram.spec（Bot API 打桩）
  - [ ] 桌面端三平台构建、`scripts/smoke-desktop.sh`、desktop/updater.spec
  - [ ] M5 DoD（用户）
