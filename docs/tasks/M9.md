# M9 任务 — 模型配置与体验

> status 取值：todo | doing | blocked | review | done | dropped
> 上位：`docs/prd/M9.md` · `docs/PRD.md` v1.12 §M9 · `docs/spec/M9.md` · 复核 `docs/qa/M9-prd-review.md`
> 用户规矩：**先推进功能，测试验证类最后统一查漏补缺**——验证待办归 TASK-M9-012；守卫与纯函数单测随任务写（守卫先造违规证明会红）。
> 顺序：000 → 001 → 002 → 003 → 004 → 005 → 006（模型链路）；007 → 008（国际化）；009 → 010（TUI）；011 收口；012 最后。
> 000 是**清理先行**：M9 要改的正是这块，先把两套凭据来源与越界回落理顺，再往上盖。

**M9 DoD**：见 `docs/PRD.md` §M9 末尾。→ 只有用户能做。

### TASK-M9-000 · 清理先行：凭据单一来源、不回落别家 key
- status: todo
- prd: PRD-M9-002
- spec: SPEC-M9-002（取舍-2）
- 实现:
  - [ ] BUG-M9-002 复现测试：会话切到没配 key 的 provider 时，请求带的是默认 provider 的 key、发往别家地址
  - [ ] `loadConfig` 结束时把 `model.api_key / base_url / capabilities` 与 `DOMI_BASE_URL` 并入 `providers[model.provider]`；session `buildProvider` / `checkCredential` / `previewSwitch` 只查 `providers[id]`
  - [ ] 缺 key → `MissingCredentialError(provider)`，不回落
  - [ ] 过时注释（`session.ts` 「配置里只有一套凭据」）

### TASK-M9-001 · 厂商模板与适配器
- status: todo
- prd: PRD-M9-002
- spec: SPEC-M9-002（取舍-1）
- 前置: TASK-M9-000
- 实现:
  - [ ] `config/src/vendors.ts`：openai / anthropic / deepseek / gemini / custom → 协议、默认地址、默认能力、惯用环境变量、adapter；默认模型常量
  - [ ] `model/src/factory.ts` 按 adapter 选 SDK；去掉 `@ai-sdk/google`（对话与 embedding）；`ProviderKind` → `Protocol`；`MODEL_FAMILY / KNOWN_MODELS / COMPATIBLE_DEFAULT_BASE` 删除
  - [ ] 环境变量：`DOMI_<ID>_API_KEY` + 模板惯用名；`DOMI_API_KEY` 只给默认模型所在的 provider
  - [ ] `guard:providers` 允许文件与扫描范围按取舍-1 改；先造违规（apps/web 里写一个厂商名）证明会红

### TASK-M9-002 · Provider 配置结构与写入
- status: todo
- prd: PRD-M9-002
- spec: SPEC-M9-002（取舍-2、3）
- 前置: TASK-M9-001
- 实现:
  - [ ] schema：`providers.<id>` 加 `name / vendor / protocol / enabled / capabilities`；id 正则；`config/src/providers.ts` 规整成 `ResolvedProvider`，旧键推断
  - [ ] `config.set` 白名单改成按 id 的字段集 + 整条删除（连带 secrets）；默认模型所在 provider 停用 / 删除 → `DEFAULT_PROVIDER`
  - [ ] `domi doctor` 列出推断出来的条目
  - [ ] daemon `provider.vendors`；`config.get` 返回规整后的 providers

### TASK-M9-003 · 模型探测与 model.list
- status: todo
- prd: PRD-M9-001, PRD-M9-003
- spec: SPEC-M9-001（取舍-6）
- 前置: TASK-M9-002
- 实现:
  - [ ] `model/src/probe.ts`：两种协议的请求构造、响应解析（含 Anthropic 翻页游标）、非对话模型规则表
  - [ ] `runtime/src/model-catalog.ts`：并发、10s 超时、降级、10 分钟缓存 + 指纹失效、可注入 fetch 与时钟
  - [ ] `model.list` 新形状（`refresh`、`providers` 状态）；替换 `runtime/src/models.ts` 的旧拼装

### TASK-M9-004 · 默认模型、归属解析与会话恢复
- status: todo
- prd: PRD-M9-003
- spec: SPEC-M9-003（取舍-4、5）
- 前置: TASK-M9-003
- 实现:
  - [ ] BUG-M9-001 复现测试：切过模型的会话重开后回到默认模型
  - [ ] `model.switch` 加可选 `provider`，SCHEMA_VERSION 12，`legacy-v12.jsonl`；重新生成协议文档与 API 快照
  - [ ] `runtime/src/model-resolve.ts` + `model.resolve`；session 构造时回放最后一条 `model.switch`；provider 不可用 → 默认模型 + `session.notice`
  - [ ] 能力差集按目标条目的 provider 能力（含覆盖）计算

### TASK-M9-005 · Web：模型供应商设置页与模型下拉
- status: todo
- prd: PRD-M9-002, PRD-M9-003
- spec: SPEC-M9-002, SPEC-M9-003
- 前置: TASK-M9-004
- 实现:
  - [ ] `views/settings/Providers.tsx`：列表、新增 / 编辑 / 删除 / 启停、厂商模板带出默认值、能力开关、手填模型、重新探测、每个模型「设为默认」
  - [ ] Composer 模型下拉：按 provider 分组、搜索、来源标注；「其他…」只填模型名，走 `model.resolve`（候选 / 无命中提示）

### TASK-M9-006 · TUI：/model
- status: todo
- prd: PRD-M9-003
- spec: SPEC-M9-003
- 前置: TASK-M9-004
- 实现:
  - [ ] `/model` 无参数 → 按 provider 分组的模型弹层；带参数 → `model.resolve`，多处命中列候选；去掉 provider 参数

### TASK-M9-007 · i18n 包、守卫与错误 key
- status: todo
- prd: PRD-M9-004
- spec: SPEC-M9-004（取舍-7）
- 实现:
  - [ ] `packages/i18n`：`t()`、插值、简单复数、`zh.ts`（源）/ `en.ts`、locale 解析（浏览器 / 环境变量）
  - [ ] `scripts/check-i18n.ts`（`guard:i18n`）：键集与参数名一致、端代码无中文字面量（允许清单）；先造违规证明会红
  - [ ] daemon：`InvalidInputError` / `fail()` 带 `messageKey + params`；client-core `formatError`；`ui.locale` 进 schema 与白名单

### TASK-M9-008 · i18n 文案迁移与语言设置
- status: todo
- prd: PRD-M9-004
- spec: SPEC-M9-004
- 前置: TASK-M9-007
- 实现:
  - [ ] Web 全部文案走 `t()`；设置页「界面语言」三选一，即时生效
  - [ ] TUI 全部文案走 `t()`；golden 快照固定 zh，另录 en 一组（80 列）
  - [ ] CLI 面向用户的输出走 `t()`
  - [ ] identity 提示层：用用户使用的语言回复

### TASK-M9-009 · TUI 双渲染器
- status: todo
- prd: PRD-M9-005
- spec: SPEC-M9-005（取舍-8）
- 实现:
  - [ ] `render/viewport.ts`：条目 → 显示行（CJK 宽度、折行）→ 切片、滚动条几何、自动跟随状态机（纯函数）
  - [ ] fullscreen：备用屏、固定底部、滚动条、PgUp/PgDn/Ctrl+Home/Ctrl+End、滚轮（SGR 1006）、「N 条新消息」、Ctrl+O 倒进原生回滚
  - [ ] classic：已完成条目进 `<Static>`
  - [ ] `tui.renderer` / `tui.mouse` / `DOMI_TUI_RENDERER`；非 TTY / dumb / 屏幕阅读器强制 classic；首帧前失败回退并记住

### TASK-M9-010 · TUI 输入区
- status: todo
- prd: PRD-M9-005
- spec: SPEC-M9-005
- 前置: TASK-M9-009
- 实现:
  - [ ] 空输入无占位、无 `›`；上下横边框；按键提示并入底部快捷键行；Enter / Shift+Enter / Ctrl+J / Alt+Enter 不回退

### TASK-M9-011 · 收口：文档与交接
- status: todo
- prd: PRD-M9-001, PRD-M9-002, PRD-M9-003, PRD-M9-004, PRD-M9-005
- 实现:
  - [ ] `docs/parity-checklist.md`、`HANDOFF.md`、`NEXT.md`、`README.md`、文档站配置页更新
  - [ ] 复核报告里 A2-* 的收尾（本机残留由用户在 Mac 上清）

### TASK-M9-012 · 验证补齐（最后做）
- status: todo
- prd: PRD-M9-001, PRD-M9-002, PRD-M9-003, PRD-M9-004, PRD-M9-005
- 实现:
  - [ ] 逐条 AC 写能证伪它的测试（验收方式里点名的 spec 文件）；M9 进 `check-ac-coverage` 的 `ACTIVE`
  - [ ] 全量 `pnpm check`（干净副本 + `--frozen-lockfile`）

---

## 缺陷与待优化登记

### BUG-M9-001 · 会话里切过的模型，重开会话就丢了
- status: todo
- 发现：2026-09-18 复核（读代码）。`session.ts` 构造时只读 `config.model.*`，全仓没有回放 `model.switch`；事件也不带 provider，想恢复也恢复不出来。修在 TASK-M9-004。

### BUG-M9-002 · 切到没配 key 的 provider 时，把默认 provider 的 key 发给了别家
- status: todo
- 发现：2026-09-18 复核（读代码）。`session.ts` `buildProvider`：`apiKey: other?.apiKey ?? m.apiKey`，注释写「没配就沿用默认的」。
  后果：例如切到 deepseek 而没配它的 key，请求带着 Anthropic 的 key 发往 api.deepseek.com。修在 TASK-M9-000。
