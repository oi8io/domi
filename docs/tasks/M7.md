# M7 任务 — 会写代码

> status 取值：todo | doing | blocked | review | done | dropped
> 上位：`docs/prd/M7.md`（再批准门）· `docs/spec/M7.md` · `docs/adr/024` · `docs/adr/025` · `docs/adr/026`
> 用户规矩：**先推进功能，测试验证类最后统一查漏补缺**——「验证待办」都归到 TASK-M7-011。

**M7 DoD**：在一个非 domi 的真实仓库与 domi 仓库里各完成一个真实编码任务（issue → 带测试的本地提交；隔离 worktree；有成功的验证记录；
用户只做审批与最终审阅）；用 `domi eval mine` 从 domi 历史生成 ≥ 20 道题并跑完一轮。→ 只有用户能做。

### TASK-M7-001 · 编码工具集
- status: done
- prd: PRD-M7-001
- 实现:
  - [x] file-list（git ls-files / 内置排除表）、FileStamps
  - [x] fs.edit、fs.glob、fs.grep（rg / builtin）
  - [x] 后台 shell.exec、shell.output、shell.kill；超长输出落 ~/.domi/outputs/
  - [x] 注册进会话；config 模板与 doctor（rg 状态）
- 笔记（2026-09-16）：冒烟（桩）——没读先改 → not_read；old 两处 → ambiguous 带行号；没找到 → not_found 带最近的行；
  读后被外部改 → fs.edit / fs.write 都 stale 且文件不动；rg 与内置后端结果一致（`DOMI_GREP_BACKEND=builtin` 强制内置）；
  后台 job 增量输出、kill 后 `sleep` 子进程一个不剩；300KB 输出只回头尾，全文落盘且 fs.read 能读、工作目录外写不了。

### TASK-M7-002 · 规矩文件、项目目录、信任
- status: done
- prd: PRD-M7-002
- 实现:
  - [x] project.ts：仓库根、AGENT.md / AGENTS.md 层、trust.json、workspace.trust 事件
  - [x] SkillOverlay：项目级 Skill
  - [x] `domi init --project`、`domi trust`；本仓库 .gitignore 调整
- 笔记（2026-09-16）：冒烟（桩）——非交互：workspace.trust{trusted:false, source:default}、规矩与项目 Skill 都不进请求、不写 trust.json；
  答「信任」：根与子目录两级规矩都进（同目录 AGENT.md 优先于 AGENTS.md）、项目 Skill 进清单且 skill.load 读得到；下个会话 source:stored。
  仓库 `.domi/config.yaml` 里写的放行规则不生效。**坑**：家目录里开会话时 `~/.domi` 是数据目录不是项目目录，已排除（三个 e2e 测试据此发现）。

### TASK-M7-003 · 钩子
- status: done
- prd: PRD-M7-003
- 实现:
  - [x] config `hooks`；HookRunner；ToolRegistry 的 pre / post 端口；stop 钩子
  - [x] `hook.run` 事件（SCHEMA_VERSION 10，legacy-v10 fixture）
  - [x] 两个示例钩子（`domi hook commit-msg` / `domi hook secrets`）与模板
- 笔记（2026-09-16）：冒烟（桩）——提交信息带 Co-Authored-By → commit-msg 拦下（hook_blocked，理由回给模型）；
  暂存区有 `sk-ant-…` → secrets 拦下；普通命令放行；300ms 超时的 pre 钩子被杀并按拦截处理、文件没写；
  post 钩子输出附在结果的 `hooks` 字段；stop 钩子退出码 3 只记录。钩子只从 config.yaml 的 `hooks` 读。

### TASK-M7-004 · 完成前验证
- status: done
- prd: PRD-M7-004
- 实现:
  - [x] kernel verifyState + beforeComplete 端口；buildContext 渲染 verify.required
  - [x] config `verify`；指标快照 / 状态栏 / 轨迹三态
- 笔记（2026-09-16）：冒烟（桩）——没改文件：clean、不提醒；改了不验：提醒 2 次后 `final` 结束、状态 unverified；
  提醒后跑了 `echo test ok`：verified；验证失败：failed 并提醒；`verify.command` 生效；`verify.enabled:false` 不提醒。
  提示在下一次请求里是一条 `[运行时提示]` 用户消息。状态栏（TUI / Web）显示「已改未验 / 已验证 / 验证失败」。

### TASK-M7-005 · 计划模式
- status: done
- prd: PRD-M7-005
- 实现:
  - [x] PermissionConfig.mode；mode.switch；plan.submit 与 plan.proposed / plan.decided
  - [x] 协议 session.mode；TUI /plan /act；Web 开关；批准后转长任务
- 笔记（2026-09-16）：冒烟（桩）——计划模式下 fs.write 被拒（source: mode，提示用 plan.submit）；plan.submit 在执行模式下不发给模型；
  驳回带意见（Web 驳回按钮会带上填的意见；TUI 里 y 是「按默认值批准」）；批准后自动 mode.switch→act、之后 fs.write 按规则放行；
  选「转成长任务」：步骤生成 agent-step DAG 并经 validateSpec，runId 进 plan.decided；重开会话仍在计划模式；没人审批 = 未批准。
  另：会话的 elicitation 回答在「拒绝」时也带内容，转给 MCP server 前剥掉（MCP 规范里只有 accept 带内容）。

### TASK-M7-006 · worktree 与改动审阅
- status: done
- prd: PRD-M7-006
- 实现:
  - [x] worktree.ts：创建 / diff / discard / restore / apply / 清理
  - [x] 协议 session.create isolate、worktree.*；删除会话的保护
  - [x] Web「改动」面板；TUI /changes /apply；CLI `domi --isolate`
- 笔记（2026-09-16）：冒烟（桩，真 git）——非 git 目录拒绝；在仓库子目录里建隔离会话，会话 cwd 落在 worktree 的同一子目录；
  模型改了两个文件后原仓库 `git status` 仍干净；丢弃 / 撤销丢弃生效，越界路径被拒；带回：不批准 → 原仓库不动；
  原仓库相关文件有未提交修改 → 拒绝并列出文件；批准 squash → 原仓库多一个提交、内容正确；
  worktree 里有没提交的改动时删会话被拒；删会话后目录清掉、分支保留，恢复会话时按分支重新挂上。
  Web：会话列表下方「新建隔离会话」（可填仓库路径），隔离会话里有「查看改动」面板；TUI：`domi --isolate`、/changes /discard /undo /apply。

### TASK-M7-007 · 代码结构理解
- status: done
- prd: PRD-M7-007
- 实现:
  - [x] packages/codeintel：outline、diagnostics（LanguageService 常驻）
  - [x] code.outline / code.diagnostics 工具
- 笔记（2026-09-16）：冒烟——单文件提纲 ~100ms（含首次加载 TypeScript）；整个 packages/ 目录按 16KB 截断并说明；
  非 TS/JS 返回 unsupported；domi 仓库里 code.diagnostics 第一次 ~300ms、第二次 1ms；没有 tsconfig 的目录用宽松默认选项，
  类型错误与 JS 语法错误都能报出，文件改好后下一次调用归零。
  **坑**：Bun 的自动安装会把 `typescript` 解析到缓存里的 7.x（没有 JS API），现在只认项目 node_modules 里的，否则用随包的。
  已知限制：单文件发行版里随包的 TypeScript 读不到 lib.d.ts，项目里没装 typescript 时 diagnostics 会报找不到全局类型（记入 TASK-M7-011 验证）。

### TASK-M7-008 · git 历史出题
- status: done
- prd: PRD-M7-008
- 实现:
  - [x] mine.ts：挑提交、生成题目、双向验证、报告
  - [x] `domi eval mine`；L2 harness 支持 setup 与 tests 目录
- 笔记（2026-09-16）：冒烟——fixture 仓库 4 个提交：只改测试的不算候选；父提交上已通过的丢弃并写明原因；
  两个合格提交生成题目（工作区 = 父提交、tests/ = 该提交的测试、solution/ = 该提交的源码），题面里去掉了 Co-Authored-By 行；
  L2 harness 用这些题：放入 solution 的替身全部通过、什么都不做的替身全部失败；准备命令失败有明确报错。
  domi 自己的历史里有 66 个候选提交（依赖准备默认 `pnpm install --frozen-lockfile`，每题一次，慢）。
  `check-ci-no-l2` 同时拦 `eval mine`（造违规确认会红）。顺带修了 BUG-M6-001（见 docs/tasks/M6.md）。

### TASK-M7-009 · 任务预算
- status: done
- prd: PRD-M7-009
- 实现:
  - [x] config / 协议 / DAG 的 budget；ToolRegistry gate；budget.warn / budget.decided
- 笔记（2026-09-16）：冒烟（桩）——上限 2 次工具调用：第 2 次时落 budget.warn，第 3 次调用执行之前暂停问人；
  选停止 → 这次调用 budget_stop、本轮以 stopReason `budget` 结束（剩下的调用配上「未执行」结果）；
  选继续 → 上限放宽一半（budget.decided continue），再到顶可以设新上限（raise）；会话级上限（协议 session.budget / TUI `/budget`）
  落成 budget.decided，重开会话照样生效；没配上限时没有任何事件。DAG 顶层 `budget` 作用于每个 agent-step 节点。
  顺带：config 增加 `pricing`（与内置价目合并），daemon 里的会话终于有了花费（之前状态栏一直是 —），金额上限靠它。
  Web 端还没有设上限的入口（TUI 与协议有），记为 OPT-M7-001。

### TASK-M7-010 · 审阅子 agent
- status: done
- prd: PRD-M7-010
- 实现:
  - [x] review.ts、review.report 工具、review.findings 事件
  - [x] `domi review`；协议 review.start；Web 发现列表（会话里按文件分组；会话头部「审阅改动」按钮）
- 笔记（2026-09-17）：前一个会话写到一半没提交，本会话接手补完（Web 部分、参数解析收尾、协议文档重新生成）。
  冒烟（桩）——父会话先聊一轮再发起审阅：审阅会话的事件里没有父会话的输入与回复；未跟踪文件进了 diff；
  配置里放行了 fs.write，审阅会话里仍被范围拒掉（文件没写出来）；review.report 落 review.findings；父会话轨迹里多一条 task.spawn；
  非 git 目录 → INVALID_PARAMS「不在 git 仓库里」。`domi review` 命令行没在真终端里跑过（归 TASK-M7-011）。

### TASK-M7-011 · 验证补齐（最后做）
- status: done（只剩 M7 DoD，要用户那边做）
- prd: PRD-M7-001, PRD-M7-002, PRD-M7-003, PRD-M7-004, PRD-M7-005, PRD-M7-006, PRD-M7-007, PRD-M7-008, PRD-M7-009, PRD-M7-010
- 验证待办:
  - [x] capability/coding-tools.spec · runtime/project-rules.spec · runtime/hooks.spec · runtime/verify-gate.spec
  - [x] runtime/plan-mode.spec · runtime/worktree.spec（+ Web 渲染快照）· capability/code-intel.spec（含第二次更快的基准）
  - [x] eval/mine.spec（fixture 仓库）· runtime/budget.spec · orchestrator/review.spec
  - [x] check-ac-coverage 把 M7 纳入强制范围（`ACTIVE = /^PRD-M(?:[01]|[78])-/`）：M0 / M1 / M7 / M8 共 190 条 AC 全部有测试点名
  - [ ] M7 DoD（用户）
- 笔记（2026-09-18）：39 条 AC 全部有测试点名。几条 spec 的实际落点与计划不同，按「就近」原则放：
  - PRD-M7-001 → `packages/capability/test/coding-tools.spec.ts`（都经 ToolRegistry 跑：记录本、权限、事件挂在 registry 上；rg 与内置两个后端各跑一遍）
  - PRD-M7-002 → `packages/runtime/test/project-rules.spec.ts`；AC-6 的 `domi prompt dump` 在 `packages/cli/test/run.spec.ts`（见 BUG-M7-001）
  - PRD-M7-003 → `packages/runtime/test/hooks.spec.ts`（AC-1…AC-5，真起 sh，超时强杀连孙进程一起验）；
    AC-6 的两个示例钩子在 `packages/cli/test/hook-examples.spec.ts`（先造违规，最后一条经 runHookCommand 真跑 `domi hook commit-msg`，见 BUG-M7-002）
  - PRD-M7-004 → `packages/runtime/test/verify-gate.spec.ts`；AC-3 的两端渲染在 `apps/tui/test/statusbar.spec.tsx`、`apps/web/test/render.spec.tsx`（见 BUG-M7-003）
  - PRD-M7-005 → `packages/runtime/test/plan-mode.spec.ts`（转长任务的 DAG 经 `@domi/orchestrator` 的 validateSpec）
  - PRD-M7-006 → `packages/daemon/test/worktree.spec.ts`（不是 runtime：审阅 / 带回 / 删除保护都在 daemon 的方法表里；「原仓库一个字节不变」用整棵树的指纹断言）；
    Web 渲染快照在 `apps/web/test/views.spec.tsx`（`__snapshots__/views.spec.tsx.snap`），TUI 命令解析在 `apps/tui/test/commands.spec.ts`
  - PRD-M7-007 → `packages/codeintel/test/code-intel.spec.ts`（基准：第二次 < 第一次的一半）
  - PRD-M7-008 → `packages/eval/test/mine.spec.ts`（fixture 仓库在测试里现造：收一题、两种丢法、两种非候选；跑题用「照抄参考答案」的替身走 runOne，不调模型）
  - PRD-M7-009 → `packages/runtime/test/budget.spec.ts`（「暂停在越限后的第一个调用之前」断言的是副作用：日志行数）
  - PRD-M7-010 → `packages/daemon/test/review.spec.ts`（不是 orchestrator：review.start 在 daemon 里接线）；Web 按文件分组在 `apps/web/test/views.spec.tsx`
  补验证时找到三处与 AC 不符的地方，按缺陷修了，各自先写复现测试（见下面 BUG-M7-001…003）。

## 缺陷与待优化登记

### BUG-M7-001 · `domi prompt dump` 看不到仓库规矩层
- status: fixed（dump 与会话共用 `projectRulesLayer`；信任过的仓库多一层 project.rules 并列出来源文件，没信任时给一句说明）
- 现场: PRD-M7-002 AC-6 说 dump 能看到规矩层，但 `packages/cli/src/run.ts` 只拼了内置层与 config 的层
- 测试: cli/test/run.spec.ts「BUG-M7-001」

### BUG-M7-002 · commit-msg 钩子漏掉单独一段 `-m` 里的署名
- status: fixed（`-m` / `--message` 的每一段单独拿出来按行首匹配；单双引号、`--message=` 都认）
- 现场: `git commit -m "feat: x" -m "Co-Authored-By: …"` 放行了——规则按行首匹配，而署名在命令这一行的中间。模型最常用的就是这种写法
- 测试: cli/test/hook-examples.spec.ts「BUG-M7-002」

### BUG-M7-003 · 超长的失败输出把失败的测试名截掉了
- status: fixed（`truncateOutput` 除了头尾，再从省略的中段按原顺序摘最多 40 行像失败 / 报错 / 报错位置的行；每行只看前 1000 字符，超长单行不会拖慢）
- 现场: PRD-M7-004 AC-4 要求保留失败的测试名与首个错误位置；原来只留头尾各 20KB，测试跑得长时失败恰好在中间
- 测试: runtime/test/verify-gate.spec.ts（AC-4）· capability/test/coding-tools.spec.ts「BUG-M7-003」

### OPT-M7-001 · Web 端没有设用量上限的入口
- status: todo
- 现在只有 TUI `/budget` 与协议 `session.budget`；到顶时的询问 Web 能答
