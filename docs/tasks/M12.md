# M12 任务 — 体验修订

> status 取值：todo | doing | blocked | review | done | dropped
> 上位：`docs/prd/M12.md`（COMMITTED 2026-09-23）· `docs/spec/M12.md`
> 用户规矩：**测试先行**；遇到阻塞不中断——记 blocked + 笔记，直接做下一个。
> 执行顺序：000（SP）→ 003 → 001 → 002 → 004。

**M12 DoD（只有用户能做，最后统一手测）**：
- 模型切换只有下拉，无输入框；空清单时禁用 + 引导
- 对话框里能切三档确认模式，A 会话改了 B 会话不受影响
- 新任务直接出计划，有不确定点弹多 tab 确认框，无疑问直接跑
- 改会话标题后右上角和侧边栏同步

---

### TASK-M12-000 · SPEC/任务拆分（PRD-全册）
- status: done
- prd: PRD-M12-001…004
- spec: SPEC-M12.md
- 说明: PRD 已 COMMITTED，本任务拆 SPEC + tasks。M12-003 改名同步已实测工作（bu 浏览器 2026-09-23）。

### TASK-M12-003 · 标题同步 + 空标题口径统一（PRD-003）
- status: done
- prd: PRD-M12-003（AC-1/AC-3）
- spec: SPEC-M12-003
- 前置: 无
- 说明: 改名同步已工作（实测确认）。剩 SessionTitle 里 `title === '' ? tr('common.untitled') : title` 死分支——daemon listSessions 已回退 firstInput，前端 title 永远非空。清理死分支，空标题以 daemon 返回为准。
- 测试先行:
  - [ ] SessionTitle 渲染：title prop 为非空字符串时显示它（无"未命名"分支）
  - [ ] title prop 为 undefined 时不崩（兜底空串）
- 实现:
  - [ ] SessionTitle 删 `title === '' ? tr('common.untitled') : title`，直接 `{title ?? ''}`
  - [ ] 跑 Web SSR 测试确认无回归
- DoD:
  - [ ] 改名后右上角 + 侧边栏同步（已实测，写进 DoD 手测清单）
  - [ ] 空标题显示首条输入前 40 字（与侧边栏一致）
- 笔记（2026-09-23 对账）：代码在 6a11199（SessionTitle 空分支清理）；任务条目当时没勾，体检时补记状态。DoD 的手测仍归用户。

### TASK-M12-001 · 模型纯下拉（PRD-001）
- status: done
- prd: PRD-M12-001（AC-1…AC-6）
- spec: SPEC-M12-001
- 前置: 无
- 说明: Web ModelSwitch 删手填分支；TUI /model 只开选择器；model.resolve RPC 删除。
- 测试先行:
  - [ ] Web ModelSwitch SSR：无 `<input>` 元素；只有 `<select>`
  - [ ] Web ModelSwitch：models.length=0 时 select disabled + CredentialNotice
  - [ ] TUI /model：无参开选择器；/model <名字> 不再接受手填
- 实现:
  - [ ] Web ModelSwitch 删 custom 状态 + input+datalist + resolveModel 调用
  - [ ] TUI /model <名字> 手填路径删
  - [ ] model.resolve RPC 定义 + daemon 实现 + 测试全删
  - [ ] 跑 Web/TUI 测试
- DoD:
  - [ ] 两端无模型输入框（用户手测）
  - [ ] grep 无 resolveModel 调用
- 笔记（2026-09-23 对账）：代码在 c578176。体检时补：client-core / daemon core 里两段孤儿注释删掉；TUI `/model` 测试改为断言「带参数 = 用法错误」（PRD-M9-003 AC-8 / PRD-M12-001 AC-6），PRD v1.14 划掉 M9-003 AC-3 / AC-4。

### TASK-M12-002 · 确认模式三档会话级（PRD-002）
- status: done
- prd: PRD-M12-002（AC-1…AC-8）
- spec: SPEC-M12-002
- 前置: M12-001（同 Composer 工具栏）
- 说明: permissions.review 全局 → permissions.mode 会话级；设置页删三档；对话框加三档下拉；全仓 review 清理。
- 测试先行:
  - [ ] runtime：新会话默认 on-demand；切 allow-all 后写事件；重开恢复
  - [ ] runtime：A 会话 allow-all 不影响 B 会话 on-demand
  - [ ] Web 设置页 RuntimeTab 无三档下拉
  - [ ] Web Composer 有三档选择器（标题+描述）
  - [ ] capability 权限矩阵回归（三档 × 危险清单）
- 实现:
  - [ ] config schema 删 permissions.review
  - [ ] runtime session：permissionsMode 会话级 + permissions.mode.switch 事件
  - [ ] permission.check() 读会话级档位
  - [ ] Web Composer 工具栏三档下拉
  - [ ] TUI /mode 命令 + 补全
  - [ ] Web 设置页 RuntimeTab 删三档
  - [ ] TUI SettingsOverlay 删三档
  - [ ] i18n key web.settings.review* → web.composer.mode* 等
  - [ ] 全仓 grep review 残留清零
- DoD:
  - [ ] grep 无 permissions.review / reviewMode / web.settings.review
  - [ ] 三档切换即时生效（用户手测）
- 笔记（2026-09-23 对账）：代码在 103ab33。**AC-8 未达成**：`packages/capability` 的公开接口仍叫 `reviewMode` / `ReviewMode`（permission.ts、dangerous.ts、session.ts、permission.spec），要改名为 `permissionsMode` / `PermissionsMode`。
  体检时补：TUI 设置层测试改断言「没有三档」；Web 设置页测试删 `permissions.review` 残留；新增 Web `PermissionsModeSwitch` 与 TUI `/mode` 的测试（之前两端都没有）。
  「全部放行」语义按用户 2026-09-23 拍板改为跳过所有确认，见 TASK-M12-005。

### TASK-M12-004 · 计划流改造（PRD-004）
- status: done
- prd: PRD-M12-004（AC-1…AC-6）
- spec: SPEC-M12-004
- 前置: M12-002（档位与确认弹框交互）
- 说明: 取消 plan/act；系统直接出计划；需确认弹框多 tab；无确认直接跑。
- 测试先行:
  - [ ] 权限引擎：无 plan 分支（PLAN_MODE_READONLY 删）
  - [ ] runtime：新流程有确认点 → 弹框；无确认点 + 无阻止提示 → 直接跑
  - [ ] runtime：有阻止提示（不要/别/禁止）→ 停下来先问
  - [ ] Web 确认弹框：多 tab 按问题数分
- 实现:
  - [ ] 删权限引擎 plan 分支
  - [ ] 删 session mode/setMode/AUTO_PLAN_REASON
  - [ ] 删 Composer ModeToggle
  - [ ] plan.ts 改造成计划产出→确认流
  - [ ] Web/TUI 确认弹框组件
  - [ ] 阻止提示关键词判定
  - [ ] 转长任务能力平移
- DoD:
  - [ ] grep 无 plan/act 模式概念残留
  - [ ] 新任务直接出计划（用户手测）
- 笔记（2026-09-23 第二轮）：本条收窄为 AC-1（删计划 / 执行模式），已完成；新流程拆到 TASK-M12-006…008。
- 笔记（2026-09-23 对账）：6516293 只做了 AC-1（删计划 / 执行模式）；AC-2…AC-6 的「出计划 → 多 tab 确认 → 执行」新流程**没做**，`packages/runtime/src/plan.ts` 还在但没注册。现在长目标任务直接开干，不再先规划。
  体检时补：`mode.switch` 事件类型恢复进 schema（只读旧会话，不再产生；删类型等于改历史，INV-01），对话流与轨迹里照旧显示；SCHEMA_VERSION 12 → 13。
  i18n 里计划模式的文案 key（`common.planMode`、`web.composer.planHint`、`tui.cmd.plan` 等）成了死 key，AC-1 的残留清理还要做。

### TASK-M12-005 · 体检止血（2026-09-23）
- status: done
- prd: PRD-M11-005, PRD-M12-002, PRD-M12-004
- 说明: M10 收口时 `pnpm check` 全绿（1288 / 0）；从 M11 起 lint（7234052）、typecheck（c44347d）、protocol（3be34ab）相继变红，M12 后 5 道门禁红、25 条测试失败。仓库一直没有 remote，CI 从没跑过，所以没人发现。本任务把它拉回全绿。
- 实现:
  - [x] 用户在 Mac 上加 origin（git@github.com:oi8io/domi.git）并推送；推前扫过全部历史，只有测试用的假 key
  - [x] BUG-M11-001 shell 指纹绕过（见下）
  - [x] BUG-M11-002「按需」档下 allow 规则不算数（见下）
  - [x] 「全部放行」= 跳过所有确认（用户拍板），PRD v1.14 回写 M11-005 / M12-002
  - [x] BUG-M12-001 协议：SCHEMA_VERSION 13、`mode.switch` 类型恢复、legacy-v13 fixture、重新生成 protocol.md / schema.json / .api.md
  - [x] BUG-M10-001 AC 覆盖守卫的正则没包含 M10
  - [x] typecheck（`server.allowedOrigins` 默认值）、biome、总 PRD 回写 M11 补记 + M12 章 + 上游 AC 划线
  - [x] 失败测试逐条修：属于 M12 改动过时的改断言；属于回归的修代码
- 笔记（2026-09-23）: 本次只止血，不推进 M12-004 的新流程。

### TASK-M12-006 · 问题框 ask.user（PRD-004 AC-7）
- status: done
- prd: PRD-M12-004（AC-7）
- spec: SPEC-M12-004 第二轮 取舍-1
- 测试先行:
  - [x] protocol：questions schema 边界（1–4 题、2–4 选项、header ≤12）；questionsToForm 带扩展键与降级字段；normalizeAnswers 认两种回答
  - [x] runtime：ask.user 走询问通道，回答进工具结果；没人能回答 → decline 且不替人选
  - [x] Web：多 tab、选项 a–d、其他输入、最后核对页；单选选中自动跳下一题
  - [x] TUI：纯函数状态机（←→ 切题、↑↓ 移动、空格 / 回车选、其他进入输入、提交页回车提交、n 拒绝）+ 渲染
- 实现:
  - [x] `@domi/protocol` questions.ts
  - [x] runtime `ask.user` 工具 + 内置 allow
  - [x] Web QuestionsDialog；TUI QuestionsDialog + 按键路由
  - [x] 对话流把 ask.user 调用 / 结果渲染成问答

- 笔记（2026-09-23）：契约在 `packages/protocol/src/questions.ts`（schema、`questionsToForm`、`questionsOf`、`normalizeAnswers`、`answerText`）；交互状态机在 `packages/client-core/src/questions.ts`，Web（`apps/web/src/QuestionsDialog.tsx`）与 TUI（`components/QuestionsDialog.tsx` + `keys.ts` 的 `questionKey` + `questions-state.ts`）共用。`ask.user` 以 INTRINSIC_RULES 内置放行、排在用户规则后面（用户写的精确 deny 照样生效，有测试）。询问的 capabilityId 保持 `ask.user`，不改成 `ask.input`。对话流里调用读成问题、结果读成「标签：答案」。
  已知限制：TUI 的按键手感只能真终端手测（进 DoD）；Telegram 桥接走降级表单。
### TASK-M12-007 · 计划必须 + 审批跟档位 + 阻止提示 + 转长任务（PRD-004 AC-5/6/8/9）
- status: done
- prd: PRD-M12-004（AC-5、AC-6、AC-8、AC-9）
- spec: SPEC-M12-004 第二轮 取舍-2 / 3 / 5
- 前置: TASK-M12-006
- 测试先行:
  - [x] blockingHint 中英正反例
  - [x] 任务会话：没计划时写文件被拦（plan_required，这一轮不结束）；写了计划后放行；自由会话不拦
  - [x] always-ask：计划要批准，驳回带意见；on-demand + 阻止提示 → 要批准；on-demand 无提示 → 不问；allow-all → 不问
  - [x] 计划常驻提示词；重开会话恢复计划与批准状态
  - [x] 批准并转长任务 → startTask 收到 DAG
  - [x] SCHEMA_VERSION 14 + legacy-v14 fixture
- 实现:
  - [x] protocol `plan.update` 事件；capability ToolGate 加 reject
  - [x] runtime plan.ts 重写（plan.update 工具、审批、闸门、提示词层）；session 选项 planRequired；daemon 给任务会话打开
  - [x] 提示词约定层写明「任务先写计划、有疑问用 ask.user」
  - [x] Web / TUI 对话流显示计划卡片（步骤 + 状态）
  - [x] 项目页删「计划审阅策略」下拉，runtime 不再读 planReview

- 笔记（2026-09-23）：
  - `plan.update` 整份替换（同 TodoWrite），事件 `plan.update`（SCHEMA_VERSION 14，legacy-v14 fixture）；`plan.proposed` 不再产生
  - 闸门：ToolGate 加 `reject`（不执行、不落 permission、这一轮不结束），计划闸门排在预算闸门前；只读 + `ask.user` / `plan.update` / `review.report` 随时放行
  - 批准绑在「步骤集合（id + 文本）」上：只改状态不用重批；增删改步骤、或用户又说了「先别动」要重批。阻止提示按最近一句用户输入判定（`blockingHint`，中英正则，宁可多问）
  - 审批问题框：批准执行 / 批准并转成长任务（≥3 步且能起长任务时才有）/ 要修改（在「其他」写意见）；转长任务时已做完的步骤不进 DAG
  - 提示词：kernel 的 `prompt` 可以给函数，每次请求模型前现拼——一轮里计划更新了下一次请求就带上；`session.plan` 动态层（优先级 900、不缓存）
  - daemon：任务的顶层会话 `planRequired`（排除 `run-…` 运行会话、子 agent / 长任务节点、审阅会话）
  - 项目页删「计划审阅策略」下拉；`planReview` 存储与协议字段保留、标废弃，runtime 不再读
  - 既有测试适配：worktree / runtime-host 的任务会话脚本先写一步计划；项目页测试改断言「没有计划审阅策略」
### TASK-M12-008 · 中断续跑（PRD-004 AC-10）
- status: done
- prd: PRD-M12-004（AC-10）
- spec: SPEC-M12-004 第二轮 取舍-4
- 前置: TASK-M12-007
- 测试先行:
  - [x] client-core planProgress 投影：剩余步数、interrupted 判定
  - [x] Web 续跑条渲染与提交；TUI 提示行 + `/continue`
- 实现:
  - [x] client-core 投影；Web 续跑条；TUI 提示行与命令

### TASK-M12-009 · 状态栏显示确认模式 + 命名收尾（PRD-002 AC-8 / AC-9）
- status: done
- prd: PRD-M12-002（AC-8、AC-9）
- spec: SPEC-M12-002
- 测试先行:
  - [x] Web / TUI 状态栏显示档位，allow-all 用警示色
- 实现:
  - [x] ReviewMode → PermissionsMode 改名；计划模式死 i18n key 清掉
  - [x] 两端状态栏

- 笔记（2026-09-23）：`@domi/capability` 的 `ReviewMode` / `reviewMode` 全仓改名（grep 为 0）；删 14 个死 i18n key（计划 / 执行模式、手填模型、未命名），CLI 帮助里的 `/plan /act` 换成 `/mode` 与 `/continue`；补上缺的 `error.feature.permissionsMode`（daemon 早就在用，i18n 守卫查不到动态拼的 key）。状态栏的档位名由 client-core `permissionsModeBadge` 统一给，两端同口径。M12-002 的 AC-8 至此达成，TASK-M12-002 → done。
- 笔记（2026-09-23）：计划进度走状态栏指标（metrics.plan = total / remaining / interrupted，runtime 用纯函数 `planProgress` 从全量事件流算），不靠端上投影——端上只拿到窗口化加载的那一段（M11-009），长会话里计划可能在窗口外。
  interrupted = 计划写出之后出过 error、或工具结果 reason ∈ interrupted / not_run / budget_stop，且用户还没再说话；计划闸门拦下（plan_*）不算。
  显示条件（client-core `resumeHint`）：有没做完的步骤、空闲、没有待回答的询问。续跑 = 替用户说一句 `continuePrompt`（Web「继续」按钮、TUI `/continue`）。parity 表加了第 11–13 行。
### TASK-M12-010 · Markdown 排版对齐原型 + 轻量语法高亮（Web + TUI）
- status: done
- prd: PRD-M11-003（AC-9、AC-10）
- spec: SPEC-M11-010 · ADR-028
- 说明: 用户 2026-09-23 反馈 Markdown「显示上不够协调和清晰，跟整体主题不匹配」。先出了现在 / 建议稿的深浅对照截图，用户拍板：按建议稿落地、做轻量高亮、TUI 一起调。
- 测试先行:
  - [x] client-core：分词归类、按行切且拼回原文一字不差、别名、不认识的语言不猜；SYNTAX 深浅齐全、与 globals.css 的 --syn-* 同源、对比度 ≥ 4.5:1
  - [x] Web：标题小阶梯、代码块卡片（语言头 / 复制 / 12px）、表格卡片只留横线且 w-fit、引用同「思考」、行内代码细框；SSR 先纯文本、着色后文字不变
  - [x] TUI：标题不带 #、块间空行、代码块语言头与左边线、表格一条横线且中文按显示宽度对齐；语法色真彩色 / 16 色 / NO_COLOR 三档
- 实现:
  - [x] `apps/web/src/session/MarkdownView.tsx` 重排；修掉代码块字号缩两次（globals.css 的 `pre code` 规则删掉）
  - [x] `@domi/client-core/highlight`（lowlight + 15 种语言）+ `tokens.ts` 的 SYNTAX；Web 按需 import()，高亮器单独 chunk（gzip 24.5KB）
  - [x] TUI `Transcript.tsx` 的 BlockLine / CodeLines / tableLines；theme 加 `syntax(kind)`；依赖加 `string-width`
- 笔记（2026-09-23）: 截图对照在「Claude outputs/md-preview/」（本地忽略目录）。真机字体（PingFang / JetBrains Mono）下的观感与 TUI 真终端颜色进 M12 DoD 手测。

### TASK-M12-011 · 会话首屏没撑满屏时拉不到更早历史（BUG-M12-002，PRD-M11-009）
- status: done
- prd: PRD-M11-009（AC-2）
- 说明: 用户 2026-09-24 报 `s-1789799299292-eqd4vv` 会话页没有滚动条、拉不到完整历史。复现：该会话最后一轮只有 3 条事件（user.input「好了吗」+ 断流 error，渲染 1 行），前一轮是 7470 条事件的超大轮——尾部窗口按「不拆轮」只装最后 1 轮，首屏没撑满视口 → 没有滚动条 → onScroll 永不触发；而 daemon 先推 `session.events` 再回 RPC 响应（带 oldestSeq / hasOlder），首屏事件落地时 hasOlder 还是 false，等 `setWindowMeta` 翻转它时没有任何东西再跑 `reportRead`（items effect 依赖只有 `[items, ask]`）→ 更早历史永远拉不到。违反 PRD-M11-009 AC-2「向上滚动接近顶部自动触发加载更早一页；占位可点击兜底」。
- 测试先行:
  - [x] scroll.spec：`shouldFetchOlder` 纯函数——滚到顶（0px）/ 阈值内（79px）/ 内容没撑满屏（scrollTop 被钳为 0）且 hasOlder → 拉；阈值 80px 本身、滚下去（200px）、hasOlder=false、loadingOlder → 不拉
  - [x] session-view.spec（SSR）：hasOlder 且空闲 → 顶部占位是 `data-action="load-older"` 按钮；loadingOlder → 「正在加载」占位、无按钮；无更早 → 无占位
- 实现:
  - [x] `apps/web/src/lib/scroll.ts` 抽 `OLDER_TRIGGER_PX` + `shouldFetchOlder` 纯函数
  - [x] SessionView：`reportRead` 改用纯函数判定；抽 `fetchOlder`（占位按钮与滚动触发共用同一入口，含加载后按 `scrollHeight - prevHeight` 补回位置）；items effect 依赖加 `hasOlder`（窗口边界晚到时重评估）
  - [x] 顶部「向上滚动加载更早历史」从纯文本改成可点击按钮（AC-2 占位可点击兜底）
- 笔记（2026-09-24）：判定语义可单测，真实滚动交互（首屏自动补拉、翻页位置保持手感）进 DoD 浏览器手测；本次已用浏览器对 live daemon 实测（该会话尾部窗口 3 条 / 7470 条超大轮，修复后进页即自动补拉出滚动条）。只改 `apps/web`，daemon 无需重启。

---

## 缺陷与待优化登记

### BUG-M11-001 · shell「始终允许」指纹可被绕过（安全）
- status: done
- 命令经 `sh -c` 执行，指纹却只取按空白切出的前两个词：授权过 `npm test` 后，`npm test && rm -rf ~`、`npm test; curl … | sh` 都会自动放行；`bash -c …`、`python -c …`、`FOO=1 rm …`、`/bin/rm …`、`git -C /other push`、`git push --force` 同理。违反 PRD-M11-005 5.2 AC-5「宁可少放行不可误放」。
- 修法：`dangerous.ts` 新增 `shellCommandFingerprint`，原始命令串先整体查 shell 元字符；首词必须是裸名字且不在「删除 / 提权 / 解释器 / 下载即执行」表；第二个词不能是选项；git push/reset/clean/checkout… 与 npm/pnpm publish 等子命令不可授权；同时给了 `argv` 与 `cmd` 时按真正执行的 `cmd` 算。
- 复现测试：`packages/capability/test/dangerous.spec.ts`「命令指纹不可被绕过」六组，先红（6 fail）后绿。

### BUG-M11-002 · 「按需」档下用户写的 allow 规则不算数
- status: done
- e44aecf 起危险能力「规则 allow 仍弹确认」，M7 写代码流程每写一个文件都停下来问；`daemon/test/worktree.spec.ts` 7 条测试从那个提交起一直红（`这一轮没结束`，卡在没人回答的确认上）。
- 修法（用户拍板）：按需档下显式 allow 规则算数；每次都问档仍收紧；没写规则的危险能力仍拒。`session-kind.spec` / `session.spec` 恢复 M8 原断言。

### BUG-M12-001 · 协议改动没升版本、删了历史事件类型、文档没重新生成
- status: done
- 71a9f55 新增 `permissions.mode.switch` 却没升 SCHEMA_VERSION；6516293 把 `mode.switch` 从判别联合里删掉（legacy-v10 fixture 里就有这条）；判别联合分支数一增一减刚好还是 40，所以「数字写死」那条测试没红。
- 修法：见 TASK-M12-005。

### BUG-M10-001 · AC 覆盖守卫声称强制 M10，实际没有
- status: done
- 74e2f4b 只改了提示文案，`ACTIVE = /^PRD-M(?:[01]|[789])-/` 匹配不到 `PRD-M10-`；M10-002 AC-2 与 M10-003 AC-5 两条一直没被点名。
- 修法：正则加上 `10`；两条 AC 的已有测试补上编号（测试本身确实断言了 AC 内容）。

### BUG-M12-002 · 会话首屏没撑满屏时没有滚动条，更早历史拉不到（窗口化加载）
- status: done
- 尾部窗口只有最后几轮（`s-1789799299292-eqd4vv` 最后 1 轮仅 3 条事件 / 渲染 1 行，前一轮是 7470 条事件的超大轮）→ 内容没撑满视口 → 没有滚动条 → onScroll 永不触发；daemon 先推 `session.events` 再回 RPC 响应（带 oldestSeq / hasOlder），首屏落地时 hasOlder 还是 false，`setWindowMeta` 翻转后没人复查（items effect 依赖只有 `[items, ask]`）→ 更早历史永远拉不到。违反 PRD-M11-009 AC-2「向上滚动接近顶部自动触发加载更早一页；占位可点击兜底」。
- 修法：hasOlder 翻转时重评估「顶部取更早」（自动补拉）；顶部占位改可点击按钮（AC-2 兜底）；判定抽纯函数 `shouldFetchOlder`。见 TASK-M12-011。
