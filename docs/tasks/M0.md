# M0 任务 — 内核骨架

> status 取值：todo | doing | blocked | review | done | **dropped**
> （dropped = 被 ADR 明确降级/取消。必须在笔记里写清哪条 ADR 以及何时会被重新激活——
>   直接删任务会让可追溯链断掉，而断掉的地方正是腐蚀进来的地方）
> 上位：`docs/spec/M0.md` · `docs/PRD.md` v1.2 §M0
> 预算 10 天 = 22 个 0.5d 任务。超 12 天必须缩范围并记 ADR（PRD §0.5）
> **纪律**：测试条目永远排在实现条目之前；收工前更新本文件的"笔记"字段与 `NEXT.md`

---

## 第 1–2 天 · 把最贵的风险先打掉

### TASK-M0-001 · spike：运行时与 TUI 框架
- status: done
- prd: PRD-M0-007
- spec: SPEC-M0-008
- 前置: 无
- 预估: 0.5d
- 测试先行:
  - [ ] `scripts/check-adr-fields.ts`: 断言 `docs/adr/001-runtime-choice.md` 含四个固定字段且帧耗时为数值
- 实现:
  - [ ] Bun + OpenTUI 跑一个高频流式输出 demo，量 P95 渲染帧耗时
  - [ ] 列 native 模块兼容清单（至少覆盖 `bun:sqlite`、`@modelcontextprotocol/sdk`）
  - [ ] 写 `docs/adr/001-runtime-choice.md`，必须含 `## 若不通的退路`（退回 Ink 的具体代价）
- DoD:
  - [ ] check-adr-fields 绿 · [ ] spike 代码已删除（AGENTS.md 硬规则）· [ ] NEXT.md 已更新
- 笔记: 2026-09-14 完成，但**重新界定过**：选型结论先定为 Ink，所以"OpenTUI vs Ink 对比"失去意义，改为单侧验证「Ink 在 Bun 上能不能跑 + 架构是否契合」。数据见 `docs/adr/001-runtime-choice.md`：端到端 P95 4.576ms、零 native 依赖、ink-testing-library 在 Bun 下可用。**一项未验证**：`useInput` / raw mode 在沙箱里用 PassThrough 假 stdin 收不到按键，但 Node 下表现相同，说明是缺真 TTY 而非 Bun 问题——它是 ADR-001 退路清单的第 1 条，`TASK-M0-021` 在真终端里跑 demo 时判定。spike 代码写在 /tmp 里，仓库中无残留（AC-3）。

### TASK-M0-002 · spike：buildContext 5 万事件压测
- status: dropped
- prd: PRD-M0-007
- spec: SPEC-M0-002
- 前置: 无
- 预估: 0.5d
- 测试先行:
  - [ ] `scripts/check-adr-fields.ts`: 断言 `docs/adr/002-buildcontext-perf.md` 含 `## 实测-5万事件耗时` 且为具体毫秒数
- 实现:
  - [ ] 生成 5 万条合成事件，跑全量拼装，记 P50/P95
  - [ ] > 200ms 则同时写 `## 缓解方案` 与 `## 触发时机`（SPEC-M0-002 的重新决策条件）
- DoD:
  - [ ] check-adr-fields 绿 · [ ] spike 代码已删除 · [ ] NEXT.md 已更新
- 笔记: 2026-09-14 由 `docs/adr/005-context-strategy-as-option.md` 降级——压测不再是 M0 门禁，拼装策略改为运行期可选项（`ContextPolicy.strategy`），`PRD-M0-007` AC-2 已回写。**代价已记在 ADR-005 §Consequences，不要假装没有**：少了"> 200ms"这个可判定的数字，"要不要上增量"退回成主观判断。**何时重新激活**：① TUI 出现可见卡顿 ② 会话事件数超 2 万 ③ M2 做压缩策略 A/B 需要基线。届时把实测值写回 ADR-005 的 `## 实测` 段。

---

## 第 3 天 · 脚手架与三道防线

### TASK-M0-003 · monorepo 脚手架
- status: done
- prd: PRD-M0-006
- spec: SPEC-M0-003
- 前置: 无
- 预估: 0.5d
- 测试先行:
  - [ ] `bun test` 能跑起来（哪怕零用例）
- 实现:
  - [ ] git init + .gitignore + pnpm workspace
  - [ ] 建包：`protocol` / `store` / `model` / `kernel` / `capability`（空壳 + package.json + tsconfig）
  - [ ] tsconfig 严格档：`strict` + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`
- DoD:
  - [ ] `bun test` 与 `tsc --noEmit` 均可执行 · [ ] main 可跑（INV-07）
- 笔记: 2026-09-14 完成。bun 1.4.2 / pnpm 12.4.1 / node 22。严格档额外开了 allowImportingTsExtensions（包内用 .ts 后缀显式导入）。`pnpm check` = typecheck + guard + test 一条命令。

### TASK-M0-004 · 架构守卫：dependency-cruiser + 任务 lint
- status: done
- prd: PRD-M0-006
- spec: §5 不变量影响分析
- 前置: TASK-M0-003
- 预估: 0.5d
- 测试先行:
  - [ ] 写一个**故意违规**的 fixture（kernel 里 import `node:fs`），断言 depcruise 报错退出码非 0
  - [ ] `scripts/lint-tasks.ts`: 断言本文件每个 TASK 含 `prd:` 字段（INV-10）
- 实现:
  - [ ] `.dependency-cruiser.cjs`：规则 `no-io-in-kernel`（kernel 禁 import node:fs/net/AI SDK/store 实现）、`no-logic-in-apps`
  - [ ] `scripts/lint-tasks.ts`
  - [ ] 违规 fixture 用后即删
- DoD:
  - [ ] 两条规则在 CI 中阻断 · [ ] 违规 fixture 已删除
- 笔记: 2026-09-14 完成。守卫先红过再绿：node:fs 与 AI SDK 两个违规 fixture 都验证了 depcruise 会退出非 0。踩到一个坑——dependency-cruiser 把 `node:fs` 归一成 `fs`，规则只写 `node:fs` 会**假绿**。这正是「没被证伪过的守卫等于没有守卫」的实例。`.api.md` 改为语义快照（见 SPEC-M0-004 回写）。CI workflow 已落地。

---

## 第 4–5 天 · 事件流（M0 的地基）

### TASK-M0-005 · protocol：事件类型与快照测试
- status: done
- prd: PRD-M0-001
- spec: SPEC-M0-004 · §3.1
- 前置: TASK-M0-003
- 预估: 0.5d
- 测试先行:
  - [ ] `protocol/event-schema.spec.ts`: 已知事件解析成功；**未知 `t` 降级为 UnknownEvent 且保留原文**（不抛错）
  - [ ] `protocol/api-snapshot.spec.ts`: 公开类型 dump 为 `.api.md` 与仓库中的比对
- 实现:
  - [ ] 按 SPEC §3.1 落 zod schema 与类型
  - [ ] 生成首版 `packages/protocol/.api.md` 提交进仓库
- DoD:
  - [ ] 两个测试绿 · [ ] 改任一公开类型会让快照测试变红（手工验证一次）
- 笔记: 2026-09-14 完成，5 个测试绿。降级路径覆盖四类：未知 t / 已知 t 但字段畸形 / 非对象 / null。**`.api.md` 类型快照未做**，挪到 TASK-M0-004 一起接 CI。

### TASK-M0-006 · store：EventLog 测试先行（**本任务只写测试，实现留空**）
- status: done
- prd: PRD-M0-001
- spec: SPEC-M0-001 · §3.2 · §4.1
- 前置: TASK-M0-005
- 预估: 0.5d
- 测试先行:
  - [ ] `store/event-log.spec.ts` AC-1: 一轮完整对话后 seq 连续、无空洞、无重复
  - [ ] AC-2: 模拟 kill -9（进程外重开库）后重放，与崩溃前状态 **JSON 深比较相等**
  - [ ] AC-4: 事务中途抛异常后重开，事件表中无半条事件
- 实现:
  - [ ] `EventLog` 接口签名（仅类型，`append`/`read`/`head` 抛 `not implemented`）
- DoD:
  - [ ] **三个测试必须是红的**（红的原因是未实现，不是报错退出）
- 笔记: 2026-09-14 完成。4 条测试全红，红的原因均为 NotImplementedError（已逐条确认不是加载失败）。AC-4 的"半条事件"用循环引用参数触发序列化失败来制造事务中途异常。

### TASK-M0-007 · store：SQLite 实现
- status: done
- prd: PRD-M0-001
- spec: SPEC-M0-001 · §4.1
- 前置: TASK-M0-006
- 预估: 0.5d
- 测试先行: （复用 TASK-M0-006 的三条，本任务目标是把它们变绿）
- 实现:
  - [ ] 建表 + PRAGMA（WAL / synchronous=NORMAL / STRICT）
  - [ ] `append` 单事务内分配 seq；`read` 走未知类型降级路径
- DoD:
  - [ ] TASK-M0-006 三条全绿 · [ ] main 可跑
- 笔记: 2026-09-14 完成，4 条红全绿。三个不显然处写在文件头注释里：seq 在事务内分配、序列化在事务内（挪到事务外会让 AC-4 假绿）、read 走降级路径。顺带落了 `redact.ts`（SPEC-M0-009 的存储侧一半），TASK-M0-019 只剩配置装载与退出码。

### TASK-M0-008 · store：append-only 的机器守卫
- status: done
- prd: PRD-M0-001
- spec: §5
- 前置: TASK-M0-007
- 预估: 0.5d
- 测试先行:
  - [ ] 在 fixture 中放一条 `UPDATE events SET`，断言 `scripts/check-append-only.ts` 退出码非 0
- 实现:
  - [ ] AST 扫描 SQL 字符串字面量与模板串，命中 `UPDATE|DELETE` + `events` 即失败（AC-3）
  - [ ] 接进 CI
- DoD:
  - [ ] 扫描脚本在 CI 阻断 · [ ] fixture 已删除
- 笔记: 2026-09-14 完成。AST 扫描而非 grep：注释里的 UPDATE events 不算违规，有测试。额外加了一条 fail-closed 规则——模板串里表名被插值时无法证明目标不是 events，一律拦；想改 sessions/meta 把表名写成字面量即可。

### TASK-M0-009 · store：三版本混合 fixture 与迁移
- status: done
- prd: PRD-M0-001
- spec: SPEC-M0-004 · §4.2
- 前置: TASK-M0-007
- 预估: 0.5d
- 测试先行:
  - [ ] `store/schema-compat.spec.ts`: 加载 `fixtures/events/legacy-v{1,2,3}.jsonl` 混合流，全部可解析且重放成功（AC-5）
  - [ ] 磁盘 schema_version **大于**代码版本时，只读打开并告警，不拒绝启动
- 实现:
  - [ ] 手写三份 legacy fixture（v2/v3 各含一个"未来"事件类型）
  - [ ] `meta.schema_version` 读写与迁移入口
- DoD:
  - [ ] 两个测试绿
- 笔记: 2026-09-14 完成。三份 legacy fixture 覆盖两类前向兼容：未来**新类型**降级保留原文、已知类型的未来**新字段**靠 passthrough 保留。后者原 SPEC 没覆盖，触发了 SPEC-M0-004 回写。

---

## 第 6 天 · 模型层与上下文

### TASK-M0-010 · model：ModelProvider 接口 + 打桩 provider
- status: done
- prd: PRD-M0-002
- spec: SPEC-M0-003 · §3.3 · ADR-004
- 前置: TASK-M0-005
- 预估: 0.5d
- 测试先行:
  - [ ] `model/stub-provider.spec.ts`: 打桩 provider 按脚本吐出 delta/tool-call/usage 序列
  - [ ] 断言 `providerOptions` 与原始 usage **原样透传**，适配层不丢字段
- 实现:
  - [ ] 按 §3.3 落接口；`StubProvider`（后续所有测试的模型替身，守 INV-08）
- DoD:
  - [ ] 测试绿 · [ ] `packages/kernel` 的 package.json 依赖中**不含** AI SDK
- 笔记: 2026-09-14 完成，6 个测试绿。StubProvider 刻意做得好用（函数式剧本能读到本轮 request、脚本用尽默认抛错以暴露「多跑了一轮」）——INV-08 不是靠自觉，是靠替身好用到没人想去调真接口。两条断言钉死 ADR-004：providerOptions 必须原样同一引用到达，usage 必须带未归一的原始字段。

### TASK-M0-011 · model：AI SDK 适配实现
- status: done
- prd: PRD-M0-002
- spec: SPEC-M0-003 · ADR-004
- 前置: TASK-M0-010
- 预估: 0.5d
- 测试先行:
  - [ ] 对 AI SDK 的流打桩，断言转换后的 `ModelEvent` 序列正确（不打真实 API）
- 实现:
  - [ ] `AiSdkProvider`：AI SDK 流 → `AsyncIterable<ModelEvent>`
  - [ ] base URL / apiKey / model 从配置注入
- DoD:
  - [ ] 测试绿 · [ ] depcruise 仍绿（AI SDK 只出现在 `packages/model`）
- 笔记: 2026-09-14 完成，9 个测试绿，全程 MockLanguageModelV4 不碰真接口（INV-08）。两个真问题是测试逼出来的：① `streamText` **构造时就会同步抛错**（空 prompt 之类），原来的 try 只包了 for await，异常会穿透到 loop 之外——已把构造挪进 try；② Anthropic 的工具名不接受点号，`fs.read` 必须编码成 `fs_read` 再换回来，编码后撞名直接报错不猜。**故意不给 AI SDK 的 tool 配 execute**：工具必须走 ToolRegistry 才有权限引擎（INV-03/INV-05），交给 AI SDK 执行等于绕开权限。

### TASK-M0-012 · kernel：buildContext 纯函数
- status: done
- prd: PRD-M0-006
- spec: SPEC-M0-002 · §3.5
- 前置: TASK-M0-005
- 预估: 0.5d
- 测试先行:
  - [ ] `kernel/build-context.spec.ts` AC-2: 同一事件流两次调用，JSON 序列化 **byte 级相同**
  - [ ] AC-3: 给定 fixture，输出 messages 可深比较断言
  - [ ] AC-1: depcruise 断言其依赖闭包不含 `node:fs`/`node:net`
- 实现:
  - [ ] 全量拼装（SPEC-M0-002 选 A）；时间/随机一律从入参取，不在函数内读
- DoD:
  - [ ] 三条绿 · [ ] depcruise 绿
- 笔记: 2026-09-14 完成，9 个测试绿。按 ADR-005 把策略做成注册点：`full` 是唯一实现，`incremental` 占位且调用即报错——**不静默降级**，否则 ADR-005 的限定会失效且无人察觉。踩到一个只有 tsc 会说话的坑：`Exclude<AnyEvent, UnknownEvent>` 收窄不了，因为 `__unparsed` 是 z.unknown() 推出来的**可选**字段；已在 protocol 加 `isKnownEvent` 并把原因写在注释里。另外补了 `scripts/check-kernel-purity.ts`——depcruise 管 import，管不了 `Date.now()`，AC-1 原本有洞。

---

## 第 7 天 · agent loop

### TASK-M0-013 · kernel：loop 骨架与事件回灌
- status: done
- prd: PRD-M0-002
- spec: SPEC-M0-007
- 前置: TASK-M0-010, TASK-M0-012, TASK-M0-007
- 预估: 0.5d
- 测试先行:
  - [ ] `kernel/loop.spec.ts`: 打桩 provider 请求一次工具调用，断言执行后结果回灌并继续
- 实现:
  - [ ] loop：buildContext → provider.generate → 事件落盘 → tool 执行 → 回灌
- DoD:
  - [ ] 测试绿
- 笔记: 2026-09-14 完成。kernel **不 import store**，改为在 `ports.ts` 声明 `EventSink` 端口（已回写 SPEC §3.2）——@domi/store 的 index 会 re-export SQLite 实现，kernel 一旦 import 就会把 bun:sqlite 拖进依赖闭包，即便只写 import type，INV-02 也会从硬边界退化成「靠写法小心」。kernel 的 dependencies 只有 @domi/protocol，有测试断言。

### TASK-M0-014 · kernel：三个终止条件
- status: done
- prd: PRD-M0-002
- spec: SPEC-M0-007
- 前置: TASK-M0-013
- 预估: 0.5d
- 测试先行:
  - [ ] fixture `20-tool-loop`: 第 20 次后停并产出 `error{recoverable:true}`（AC-2）
  - [ ] fixture `malformed-args`: 回灌解析错误重试，第 4 次终止（AC-3）
  - [ ] fixture `truncated-stream`: 产出 `error{recoverable:true}` 且事件流仍满足 M0-001 AC-1，用户可继续输入（AC-4）
  - [ ] 终止事件含三个计数器当时值
- 实现:
  - [ ] 三个计数器 + 10 分钟墙钟护栏
- DoD:
  - [ ] 四条绿
- 笔记: 2026-09-14 完成，7 个测试绿。三个计数器加进了 `error` 事件契约（可选字段，旧事件照样解析），因为只记「它停了」不记「为什么停」等于没记。一个设计决定：argParseRetries **成功一次就清零**——连续失败才是信号，累计失败不是，有专门的测试守这个语义。

---

## 第 8 天 · 权限与工具

### TASK-M0-015 · capability：权限引擎（fail-closed）
- status: done
- prd: PRD-M0-003
- spec: SPEC-M0-005 · §3.4
- 前置: TASK-M0-005
- 预估: 0.5d
- 测试先行:
  - [ ] AC-4: **默认配置下 `check()` 返回 deny**（不是 ask）
  - [ ] AC-3: 决策事件必含 `capabilityId`/`decision`/`source`/`matchedRule` 四字段
  - [ ] AC-2: 拒绝后产出 `tool.result{ok:false, reason:'user_denied'}`，模型收到语义化说明而非异常
- 实现:
  - [ ] 三级来源 default < config < user
- DoD:
  - [ ] 三条绿
- 笔记: 2026-09-14 完成。默认分支返回 deny 不是 ask——ask 看着友好，实际是把「没想过的能力」变成弹窗，用户点三次之后就不看内容了。另有一条容易漏的：配置说 ask 但没有可问的人（非交互环境）时，同样拒绝而不是放行。

### TASK-M0-016 · capability：路径穿越防护
- status: done
- prd: PRD-M0-003
- spec: SPEC-M0-005
- 前置: TASK-M0-015
- 预估: 0.5d
- 测试先行:
  - [ ] 表驱动 8 例全部被拒：`../` / 绝对路径 / 符号链接 / `..%2f` / NUL 截断 / 大小写变体 / UNC / `~` 展开（AC-5）
- 实现:
  - [ ] `resolveWithinRoot(root, p)` 一个函数收口，先 realpath 再 relative
- DoD:
  - [ ] 8 例全绿 · [ ] 代码中无第二处路径校验实现（grep 自查）
- 笔记: 2026-09-14 完成。8 类穿越表驱动全绿 + 5 个合法路径不误伤。realpath 放在最后做是关键——只做字符串 resolve 会被一个指向外部的软链接直接绕过去，测试里有这条。fs.write 要写还不存在的文件，所以 resolve 会向上找最近存在的祖先再算相对路径。

### TASK-M0-017 · capability：fs.read / fs.write
- status: done
- prd: PRD-M0-004
- spec: SPEC-M0-006
- 前置: TASK-M0-016
- 预估: 0.5d
- 测试先行:
  - [ ] AC-1: > 1MB 返回结构化截断提示（含总行数与已返回范围），不返回全文
  - [ ] AC-2: 写入前后各一条事件，含 SHA-256，据此可重建 diff
- 实现:
  - [ ] 两个 Tool，均声明 capability（INV-05 编译期）
- DoD:
  - [ ] 两条绿
- 笔记: 2026-09-14 完成。fs.read 超 1MB 返回结构化 truncated（含 totalBytes/总行数/已返回范围/续读提示），不是静默截断——模型得知道「还有更多」。fs.write 的前后指纹走 ctx.emit 交给 loop 落盘，工具不自己写 store。

### TASK-M0-018 · capability：shell.exec
- status: done
- prd: PRD-M0-004
- spec: SPEC-M0-006
- 前置: TASK-M0-016
- 预估: 0.5d
- 测试先行:
  - [ ] AC-3: 超时 120s 产出 `reason:'timeout'`，**断言子进程已不存在**
  - [ ] AC-4: 输出 > 100KB 截断为头 20KB + 尾 20KB + 省略标记（标记含被省略字节数）
- 实现:
  - [ ] 子进程组管理，超时杀整组
- DoD:
  - [ ] 两条绿
- 笔记: 2026-09-14 完成。spawn 用 detached 让子进程自成进程组，超时 kill(-pid) 连孙子进程一起杀；不这么做的话杀掉 sh 会留下仍在跑的 sleep/npm。测试用 pidfile 断言进程真的没了（kill(pid,0) 抛错）。输出截断保留头尾各 20KB——只留头会丢报错，只留尾会丢命令上下文。

---

## 第 9 天 · 配置与 TUI

### TASK-M0-019 · config：装载与凭据脱敏
- status: done
- prd: PRD-M0-008
- spec: SPEC-M0-009
- 前置: TASK-M0-007
- 预估: 0.5d
- 测试先行:
  - [ ] AC-1: 环境变量优先于 `~/.domi/config.toml`
  - [ ] AC-2: 对全量事件流与日志做 5 类凭据正则扫描，无命中
  - [ ] AC-3: 缺凭据退出码为 2，stderr 含 `error.missing_credential` 且**不含堆栈特征**
- 实现:
  - [ ] 脱敏钩子挂在 `EventLog.append` 前置（SPEC-M0-009：写入边界，不是渲染层）
  - [ ] 正则常量单点定义，`scripts/scan-secrets.ts` 与运行时共用
- DoD:
  - [ ] 三条绿 · [ ] grep 确认正则只有一处定义
- 笔记: 2026-09-14 完成，12 个测试绿。TOML 用 Bun 内置的 `Bun.TOML.parse`，不引库（已回写 DESIGN §5/§6，新增 packages/config）。环境变量优先于文件——反过来会让「我明明设了环境变量为什么没生效」变成常见困惑。退出码用 2 不是 1，让脚本能区分「配置问题」和「运行失败」。另补了 `scripts/scan-secrets.ts`（AC-2 的 CI 侧），与运行时共用 redact.ts 的同一份正则。

### TASK-M0-020 · tui：骨架与流式渲染
- status: done
- prd: PRD-M0-005
- spec: SPEC-M0-008
- 前置: TASK-M0-001, TASK-M0-013
- 预估: 0.5d
- 测试先行:
  - [ ] AC-1: 渲染流式输出、工具调用名与参数摘要（JSON 序列化后前 80 字符 + `…`）、工具结果
  - [ ] PRD-M0-002 AC-1: 提交后 ≤100ms 渲染 `<Spinner>`
- 实现:
  - [ ] 按 ADR-001 的结论选框架；`apps/tui` 不 import kernel/store（INV-02）
- DoD:
  - [ ] 两条绿 · [ ] depcruise 绿
- 笔记: 2026-09-14 完成。为了不破 INV-02，中间加了两个包：`packages/runtime`（DomiSession 门面，M0 不拆 daemon 但装配集中在这里，M3 整体搬进 daemon、TUI 一行不改）与 `packages/client-core`（nanostores 投影，不依赖 react，ADR-009）。depcruise 的 no-logic-in-apps 扩到了 model/mcp/memory/orchestrator，并加了会红的 fixture 验证。

### TASK-M0-021 · tui：确认框、退出 flush、golden 快照
- status: review
- prd: PRD-M0-005
- spec: SPEC-M0-008
- 前置: TASK-M0-020, TASK-M0-015
- 预估: 0.5d
- 测试先行:
  - [ ] AC-2: y/n 响应；连续三次渲染后 focusId 不变
  - [ ] AC-3: Ctrl+C 退出前 flush，退出后重放不丢最后一轮
  - [ ] AC-4: 40/60/80/200 四种宽度 golden 快照 **diff 为 0**
  - [ ] PRD-M0-003 AC-1: 确认框含完整待执行内容（写入 diff / 命令行）
- 实现:
  - [ ] 确认组件 + 退出钩子 + 四份快照提交进仓库
- DoD:
  - [ ] 四条绿
- 笔记: 2026-09-14 沙箱内可测的部分全部完成：四宽度 golden 快照已生成并提交（UPDATE_GOLDEN=1 重生成）；焦点与按键语义做成**纯函数**（focusIdOf / answerFromKey）放在 client-core，无 TTY 也能断言——其中「回车不等于同意，默认拒绝」是 INV-03 在交互层的延续；AC-3 的 flush 在 runtime 测试里验证（重开库最后一轮一条不少）。**status 留在 review 而不是 done**：`useInput` 在真终端的验证还没做，那是 ADR-001 退路清单第 1 条的判定点，走查清单见 `demos/m0-loop.md`，跑完写进 `docs/qa/M0.md` 再改 done。

---

## 第 10 天 · 闭环与收尾

### TASK-M0-022 · M0 DoD 走查与对账
- status: todo
- prd: PRD-M0-001..008（DoD 汇总）
- spec: 全部
- 前置: 全部
- 预估: 0.5d
- 测试先行:
  - [ ] `scripts/smoke.sh`: 冒烟脚本跑通一次最小闭环（INV-07）
- 实现:
  - [ ] 写 `demos/m0-loop.md` 并在真实项目里走通：读文件 → 改代码 → 跑测试 → 报告
  - [ ] **10 分钟对账**：拉 PRD-ID / SPEC-ID / TASK-ID / 测试 四列表，看有无空格（README 可追溯链）
  - [ ] 逐条对照 `PRD-VISION.md` §5 的 12 条不变量
- DoD:
  - [ ] demo 走通并录屏 · [ ] 对账表无空格 · [ ] NEXT.md 更新为"M0 完成，等 QA 独立会话"
- 笔记: QA 必须另开会话，只加载 PRD-VISION + docs/prd + 代码，不带本轮对话历史（AGENTS.md）
