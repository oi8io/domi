# M11 任务 — 会话体验深化

> status 取值：todo | doing | blocked | review | done | dropped
> 上位：`docs/prd/M11.md`（COMMITTED 2026-09-21）· `docs/spec/M11.md`
> 用户规矩：**测试先行**（下面「测试」条排在「实现」条之前，先让它红再写实现）；遇到阻塞不中断——记 blocked + 笔记，直接做下一个。
> 执行顺序：000 → 001 → 002 → 003 → 004 → 005 → 006 → 008（TUI Markdown）→ 007 收口。
> **PRD-M11-007（TUI 中断）不建任务**：语义等终止方案 S1–S4 拍板，PRD 只占位。

**M11 DoD（只有用户能做，最后统一手测）**：
- 打开长会话停在底部最新内容；运行中自动贴底；上翻后出现「N 条新消息」浮条，点它回底
- assistant 回复有代码块/标题/列表视觉层次；复制代码块不丢字
- tool 展开后看到完整参数/结果（不是省略号）
- 设置页切审核档位，危险能力任何档位都要确认
- 确认框里 shell 类命令的「始终允许」只对安全指纹出现一次

---

### TASK-M11-000 · 三端自启后台进程（PRD-006）
- status: done
- prd: PRD-M11-006（AC-1/AC-3/AC-4/AC-5）
- spec: SPEC-M11-007（desktop 留白）
- 前置: 无
- 说明: ensureDaemon（launcher.ts）已实现，TUI `connect.ts` 已接（L87/L178）。本任务 = 回归确认 + Web 侧「连不上的可操作指引」。**desktop 壳改造 blocked**（见笔记）。
- 测试先行:
  - [ ] launcher 既有测试回归：无锁文件 → spawn 后锁写 pid+端口；锁在 pid 死 → 重拉；端口在听 → `spawned:false`
  - [ ] Web：渲染层「连接断开/连不上 daemon」的现有提示，补一行起 daemon 的操作指引（`pnpm domid`）的存在性断言（SSR HTML 含指引文案）
- 实现:
  - [ ] 跑 launcher 既有 spec，确认全绿（回归，不新写）
  - [ ] Web 连接失败态文案补操作指引（具体落点实现时定：StartError 或 StatusBar）
- DoD:
  - [ ] 上述测试绿
  - [ ] desktop 部分记 blocked（不做）
- 笔记（2026-09-21）：launcher.spec 4/4 回归绿（ensureDaemon 已有，TUI 已接 connect.ts）。Web 侧加 lastError 启动指引（Sidebar + i18n key core.client.startDaemonHint）。desktop 壳 blocked（ADR-021 冲突 + 无 Rust）。commit 见 git log。

### TASK-M11-001 · 进入会话默认滚动到底（PRD-001）
- status: done
- prd: PRD-M11-001（AC-1/AC-3）
- spec: SPEC-M11-000、3.1
- 前置: 无
- 测试先行:
  - [ ] `shouldStickToBottom` 纯函数单测：distance<160→true；≥160→false（边界 160 本身）
  - [ ] SSR 断言：切到新会话的 SessionView 渲染无回归（现有 session-view spec 绿）
- 实现:
  - [ ] 抽 `shouldStickToBottom(distance)` 纯函数（落点：SessionView 同文件导出或 lib/）
  - [ ] SessionView：sessionId 切换/mount 时强制贴底一次（scrollTop = scrollHeight）；之后沿用距底阈值逻辑
- DoD:
  - [x] 纯函数单测绿；现有 web 测试绿
  - [x] 初始贴底交互进用户手测（SSR 测不到 useEffect 时序，SPEC-M11-000）
- 笔记（2026-09-21）：新建 apps/web/src/lib/scroll.ts（NEAR_BOTTOM_PX=160），5 单测绿；SessionView 加 pendingStick ref（sessionId 切换置 true），首次渲染强制贴底。commit 见 git log。

### TASK-M11-002 · 展开后显示完整内容（PRD-004）
- status: done
- prd: PRD-M11-004（AC-1/AC-2/AC-3）
- spec: SPEC-M11-003、3.3
- 前置: 无
- 测试先行:
  - [ ] client-core：`tool.call` 事件投影出的 TranscriptItem 带 `detail`（=JSON(完整 args)，不是 80 字截断）；`tool.result` 同理带 `detail`（完整 payload）
  - [ ] Web SSR：展开 tool 行时输出完整参数 JSON（含超过 80 字的部分），不再只输出 summary
- 实现:
  - [ ] `store.ts` TranscriptItem 加 `detail?: string`；applyEvent 的 tool.call/tool.result 分支带上
  - [ ] `Transcript.tsx`：tool `<pre>` 显示 `row.call.detail ?? row.call.summary`；result `<pre>` 显示 `row.result.detail ?? row.result.text`（保留 max-h-260 滚动）
  - [ ] reason 展开显示全文（现状，回归）
- DoD:
  - [x] 投影单测绿；Web SSR 断言绿；TUI 无折叠/截断则无需改（核查后记笔记）
- 笔记（2026-09-21）：TranscriptItem 加 detail?；tool.call/tool.result 投影带 detail（fullJson 不截断）；展开 <pre> 优先 detail。store 18/18、Web 134/134 绿。

### TASK-M11-003 · 运行中自动贴底与新消息提示（PRD-002）
- status: done
- prd: PRD-M11-002（AC-1/AC-2/AC-3/AC-4）
- spec: SPEC-M11-001、3.1
- 前置: TASK-M11-001（纯函数已抽）
- 测试先行:
  - [ ] SSR 断言：上翻离开底部后新内容来了，渲染含「N 条新消息」浮条（计数对）
  - [ ] 纯函数：距底<160 时贴底（沿用）
- 实现:
  - [ ] SessionView 跟随态：`following` 布尔 + `newCount`；onScroll 检测离开底部 → following=false；items 变化时若 !following 且新事件 → newCount+1
  - [ ] 底部浮条：`{newCount} 条新消息`，点击 → 回底、newCount=0、following=true
  - [ ] i18n 文案进 `@domi/i18n`（web.* 键，中英文）
- DoD:
  - [x] SSR 断言绿；浮条点击/回底交互进用户手测
- 笔记（2026-09-21）：新建 session/JumpBar.tsx（count>0 渲染浮条）；i18n web.chat.newItems；SessionView 追踪 awayFromBottom ref + newCount，onScroll 回底清计数。

### TASK-M11-004 · Web Markdown 渲染（PRD-003）
- status: done
- prd: PRD-M11-003（AC-1..AC-5）
- spec: SPEC-M11-002、3.2
- 前置: 无
- 测试先行:
  - [ ] `Markdown` 组件 SSR：代码块（带 ```lang）、标题、有序/无序列表、行内代码、粗体、链接、表格（remark-gfm）各自渲染出对应 HTML
  - [ ] 安全用例：`<script>alert(1)</script>`、`javascript:steal()` 链接 → 不出现在输出 HTML（不渲染 HTML）
  - [ ] 降级：渲染异常时退回纯文本 `<div className={TEXT}>`，内容不丢
  - [ ] 现有 transcript 测试不回退（reason/tool 仍纯文本）
- 实现:
  - [ ] 加依赖 `react-markdown` + `remark-gfm`
  - [ ] 新建 `apps/web/src/Markdown.tsx`（catch 降级；不加 rehype-raw）
  - [ ] `Transcript.tsx` assistant 分支用 `<Markdown text={item.text}/>`；user/reason 保持现状
  - [ ] 代码块/表格样式进 globals.css（现有 bg-code 等 token）
- DoD:
  - [x] 上述 SSR 断言绿；typecheck 绿；真实渲染走查进用户手测
- 笔记（2026-09-21）：装 react-markdown+remark-gfm；新建 session/MarkdownView.tsx（只包 assistant，不开 rehype-raw，默认 urlTransform 滤 javascript:，XSS 测试断言 script 不渲染）；globals.css 加 .md-body。Web 139/139 绿。
- 笔记（补，主题适配）：**BUG——.md-body 是死 CSS**（容器没挂该类，Tailwind preflight 清空 h1/p/ul/li/pre/table 默认样式，视觉裸奔；且 code 引用了不存在的 `--mono` 变量）。改为 react-markdown `components` prop + token 类（text-ink / bg-panel / bg-code / border-border / text-accent…），跟随深浅主题与 accent 换色；表格改 wrapper 滚动保留语义；globals.css 的 .md-body 精简为父子覆盖（pre>code 透明、li>p 归零、首尾 margin 归零）。markdown.spec 增 6 条主题断言，Web 152/152 绿。commit 22ffa6d。

### TASK-M11-005 · 审核档位（PRD-005.1）
- status: done
- prd: PRD-M11-005（AC-1..AC-5）
- spec: SPEC-M11-004、3.4、3.5、3.6
- 前置: 无
- 测试先行:
  - [ ] `capability/test/permission.spec.ts` 扩展：三档 × 能力矩阵——
    - on-demand（默认）：无规则→deny（现状回归）；危险能力规则 allow→仍 ask；规则 ask→问
    - always-ask：无规则→ask（有人则问，无人 deny）；危险能力→问
    - allow-all：无规则非危险→allow；危险能力无规则→deny、规则 allow→问
    - 危险能力**任何档位**不自动放行
  - [ ] config schema：`permissions.review` 缺省 = `on-demand`；非法值解析报错
  - [ ] 子 agent 继承父档位（scope 不回退）
- 实现:
  - [ ] 新建 `capability/src/dangerous.ts`：DANGEROUS 常量（真实 id：fs.delete/fs.write/fs.move/fs.append、shell.exec、web.fetch + mcp.*/plugin.* 前缀）+ `isDangerous()`
  - [ ] `PermissionConfig` 加 `reviewMode?: () => ReviewMode`；check() 按 3.5 语义改
  - [ ] config/schema.ts：permissions 加 `review` 枚举默认 on-demand
  - [ ] Web/TUI 设置页档位选择器（M10-004 设置入口已有）
- DoD:
  - [x] 权限矩阵单测绿；runtime 141/141 绿（集成测试已适配新语义）
- 笔记（2026-09-21）：capability/dangerous.ts（DANGEROUS_EXACT/PREFIX/isDangerous）；permission.check() 三档语义+fail-closed；config schema permissions.review 默认 on-demand；runtime session.ts 接 reviewMode；config 白名单+Web RuntimeTab 三档下拉+TUI SettingsOverlay 三档。

### TASK-M11-006 · 一次「始终允许」命令指纹（PRD-005.2）
- status: done
- prd: PRD-M11-005（AC-1..AC-5）
- spec: SPEC-M11-006、3.7
- 前置: TASK-M11-005（档位与危险清单落地后）
- 测试先行:
  - [ ] `commandFingerprint` 单测：`git status`→`git status`；`git diff HEAD~`→`git diff`；裸 `git`（无首词）→null（不可 grant）；危险词（`rm`/`sudo`…）→null
  - [ ] 引擎：指纹命中 → allow（source: session-grant，事件记录 fingerprint）；不同指纹仍 ask
  - [ ] 危险操作确认框 grantable=false（不出现「始终允许」）
  - [ ] 重开会话不残留（内存态，与 M8-016 同）
- 实现:
  - [ ] `commandFingerprint(argv)`：拆词，argv[0]+' '+argv[1]，危险词命中→null
  - [ ] `grantable()` 扩展：shell.exec 在指纹非 null 时可 grant；SessionGrant 加 fingerprint 字段；`covers()` 加 fingerprint 精确分支
  - [ ] 确认框（Web ConfirmDialog / TUI）grantable 透传；授权事件 payload 带 fingerprint
- DoD:
  - [x] 指纹矩阵单测绿；session-grant/coding-tools spec 绿
- 笔记（2026-09-21）：commandFingerprint(argv)=argv[0]+argv[1]，危险词/裸命令返回 null；grantFor shell.exec 走指纹；SessionGrant.fingerprint；covers() 指纹精确分支。dangerous.spec 8/8、permission.spec 三档矩阵全绿。确认框「始终允许」UI 透传随引擎 grantable=true。

### TASK-M11-008 · TUI Markdown 渲染（PRD-003）
- status: done
- prd: PRD-M11-003（AC-6/AC-7/AC-8）
- spec: SPEC-M11-008、3.2b
- 前置: TASK-M11-004（Web Markdown 已落地，解析器依赖已在 monorepo）
- 说明: TUI 复用 remark-parse + remark-gfm（与 Web 同一 parser），自写 mdast→ANSI 渲染层。纯文本消息零变化（AC-7），不破坏既有 golden 快照。
- 测试先行:
  - [x] 渲染层单测（bun:test，纯函数）：标题 h1-h4、有序/无序列表、代码块（含 lang）、行内代码、粗体、引用、链接、GFM 表格 → 各自产出带样式/前缀的 ANSI 行
  - [x] 纯文本零变化：不含语法（如「我先读一下 sum.js。」）→ 输出与现状逐字一致（AC-7）
  - [x] HTML 节点跳过：`<script>alert(1)</script>` 不出现在输出（INV-06 / AC-8）
  - [x] 渲染异常降级：解析抛错 → 回退原文，内容不丢（AC-8）
  - [x] 既有 TUI 测试回归：render-basics / golden / viewport 不回退
- 实现:
  - [x] apps/tui/package.json 显式声明 remark-parse / remark-gfm / unified（dependencies）
  - [x] 新建 apps/tui/src/render/markdown.ts：`remark().use(remarkGfm).parse(md)` → mdast → 遍历出带样式的行
  - [x] Transcript.tsx 的 Line assistant 分支：检测 Markdown 结构则渲染，否则保持现状
- DoD:
  - [x] 渲染层单测绿；既有 TUI 测试全绿
  - [ ] 真终端手测（标题/列表/代码块/表格的 ANSI 视觉区分）进用户 DoD
- 笔记（2026-09-21）：新建 apps/tui/src/render/markdown.ts（parseMarkdownBlocks + hasMarkdownStructure，纯函数，13 单测绿）；依赖声明 remark-parse/remark-gfm/unified（react-markdown 的传递依赖，monorepo 已装，零出站 INV-11 不破）；Transcript 的 Line assistant 分支有结构才走 MarkdownBlocks，纯文本保持现状逐字一致（AC-7，四宽度 golden 快照全过）。顺手修了 M11-005 的 golden 欠账（fake client 缺 getSettings mock + settings snapshot 缺审核三档，见 commit 7cdbd27）。TUI 110/110 绿、typecheck 绿、depcruise 无违规。

### TASK-M11-007 · 收口
- status: todo
- prd: PRD-M11-005（全册收口）
- spec: —
- 实现:
  - [ ] `pnpm check` 全绿（typecheck + guard + test + eval）
  - [ ] M11 进 `check-ac-coverage` 强制范围（M10-006 先例）
  - [ ] 更新 NEXT.md；git log --grep TASK-M11 逐条对账
- 笔记:

---

## 阻塞登记

- **PRD-M11-006 desktop 壳自启**：与 `apps/desktop` README 引的 **ADR-021（桌面端不自己拉起 domid）** 冲突；且本机无 Rust 工具链（README 明说 TASK-M5-008 未验证）。本轮不改壳——改了也构建不了验证不了（INV-07 裸奔）。PRD 方向（三端自启）已知，等 Rust 环境/壳重构再做。Web 侧本轮只做「连不上的指引」，真自启依赖壳。
- **PRD-M11-007（TUI 中断）**：等终止方案 S1–S4 拍板，不建任务。
