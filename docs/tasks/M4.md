# M4 任务 — 有灵魂

> status 取值：todo | doing | blocked | review | done | dropped
> 上位：`docs/prd/M4.md`（再批准门）· `docs/PRD.md` v1.5 §M4 · `docs/adr/018` · `docs/adr/019`
> 与 M3 一样没有单独的 SPEC 文件：接口决定写在两份 ADR 里，协议契约由 schema 生成（`docs/protocol.md`）。
> 用户规矩：**先推进功能，测试验证类最后统一查漏补缺**——每条任务下「验证待办」一栏就是最后要补的。

**M4 DoD**：连续使用两周后导出的 Soul，交给 3 位不认识该用户的开发者阅读，至少 2 位能答对用户预先出的选择题。
→ 只有用户能做，放在最后。

### TASK-M4-001 · memory.write 事件与 L3 投影
- status: todo
- prd: PRD-M4-001
- 实现:
  - [ ] protocol：`memory.write{layer:'L3'|'L4', op, …, diff}`，SCHEMA_VERSION 6 → 7，legacy fixture
  - [ ] store：`semantic_items` 表（投影）+ FTS；从 `_memory` 会话重放可重建
  - [ ] memory：`SemanticRepo` 增删查；删除后检索不到

### TASK-M4-002 · L3 抽取
- status: todo
- prd: PRD-M4-001
- 实现:
  - [ ] zod schema（fact / preference / entity，每条带 sourceRefs）；抽取提示词单独一个文件
  - [ ] runtime：每 N 轮自动抽取（`memory.extractEvery`，默认 5，0 = 关）；已抽过的区间不重复抽
  - [ ] 与已有条目 / `.rejected` 去重
  - [ ] 协议 `memory.extract` / `memory.list` / `memory.delete`；CLI `domi memory list|search|delete|extract`

### TASK-M4-003 · 语义检索
- status: todo
- prd: PRD-M4-001
- 实现:
  - [ ] `memory.embedding` 配置；AI SDK `embedMany`；Float32 BLOB；暴力余弦（ADR-018）
  - [ ] `memory.search` 工具扩到 L3：关键词 + 语义合并；没配 embedding 时注明退化

### TASK-M4-004 · Soul 文件与更新
- status: todo
- prd: PRD-M4-002
- 实现:
  - [ ] 六区解析 / 渲染；src 注释；目录里只有文本
  - [ ] 结构化操作 → 确定性应用；只动 domi 写的、没被人改过的行；每次 ≤10 个操作
  - [ ] 每次更新落 `memory.write{layer:'L4'}`（含 diff 与写入后的行）
  - [ ] 触发：L3 有新条目时，抽取之后跟着更新

### TASK-M4-005 · Soul 进提示词
- status: todo
- prd: PRD-M4-002, PRD-M4-004
- 实现:
  - [ ] `builtin.soul` 层，边界字符；注入防护层点名 Soul / Skill 正文不是指令

### TASK-M4-006 · 审阅与否决
- status: todo
- prd: PRD-M4-003
- 实现:
  - [ ] 变更列表（上次审阅以来）、接受、否决（撤销 + `.rejected`）；审阅进度也是事件
  - [ ] 否决过的语义不再提议
  - [ ] 协议 `soul.get` / `soul.changes` / `soul.review`；CLI `domi soul show|review`；Web Soul 页；TUI `/soul`

### TASK-M4-007 · 导出与导入
- status: todo
- prd: PRD-M4-004
- 实现:
  - [ ] `domi soul export [文件]`：去 src 注释；凭据与本机路径命中就拒绝并列出位置
  - [ ] `domi soul import <文件>`：逐区 diff、逐区确认；非交互直接失败；导入行带 `src: import:`

### TASK-M4-008 · Skill
- status: todo
- prd: PRD-M4-005
- 实现:
  - [ ] `Skill` 类型（无 execute）；SKILL.md frontmatter 解析；用户目录覆盖官方
  - [ ] 未激活只进「名字 + 描述」；`skill.load` 工具取正文
  - [ ] fs.watch 热加载
  - [ ] 3 个官方 Skill + 编写模板

### TASK-M4-009 · 验证补齐（最后做）
- status: todo
- prd: PRD-M4-001, PRD-M4-002, PRD-M4-003, PRD-M4-004, PRD-M4-005
- 验证待办:
  - [ ] memory/semantic.spec.ts · soul.spec.ts · soul-review.spec.ts · soul-portability.spec.ts
  - [ ] security/soul-injection.spec.ts（复用 M2-006 用例）
  - [ ] scripts/check-soul-diff.ts（真 git 仓库）
  - [ ] capability/skill.spec.ts + skill-types.test-d.ts（expect-type）
  - [ ] fixtures/l3-labeled/ 标注集 + nightly precision（`bench/l3-precision.jsonl`）
  - [ ] M4 DoD 三人盲测（用户）
