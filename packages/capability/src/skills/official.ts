/**
 * 官方 Skill —— PRD-M4-005 AC-4
 *
 * 写成字符串常量而不是随包的 .md 文件：单二进制（bun build --compile）里没有可读的包目录。
 * 用户在 ~/.domi/skills/<同名>/SKILL.md 放一份就能覆盖。
 */
import type { Skill } from '../types.ts'

const official = (name: string, description: string, requiresTools: string[], prompt: string): Skill => ({
  name,
  description,
  requiresTools,
  source: 'official',
  prompt: prompt.trim(),
})

export const OFFICIAL_SKILLS: readonly Skill[] = [
  official(
    'commit-message',
    '写 git 提交信息：说清为什么改、改了什么，一次提交一件事',
    ['shell.exec'],
    `
# 写提交信息

1. 先看改动：\`git status\` 与 \`git diff --staged\`（没暂存就看 \`git diff\`）。改动里混了几件不相干的事，先建议拆开提交。
2. 标题一行，不超过 50 个字符：\`<类型>(<范围>): <做了什么>\`。类型用 feat / fix / docs / refactor / test / chore。
   标题写结果（「会话列表支持按标题过滤」），不写过程（「修改了 list 函数」）。
3. 空一行，正文写**为什么**：原来哪里不对、为什么这么改、有没有别的做法被放弃。改了什么 diff 里看得到，不用复述。
4. 关联的缺陷或任务编号放在正文末尾。
5. 遵守仓库自己的约定（看 \`git log --oneline -20\` 的风格）；仓库说不要某类信息，就不写。
6. 提交前把信息给用户看一眼，用户确认后再执行 \`git commit\`。
`,
  ),
  official(
    'code-review',
    '审查一段改动：先找会出错的地方，再谈风格',
    ['fs.read', 'shell.exec'],
    `
# 代码审查

按这个顺序看，前面的问题没看完不看后面的：

1. **正确性**：边界值（空、零、一个、很多）、错误路径有没有被吞掉、并发下的先后顺序、资源有没有释放。
2. **契约**：公开接口、数据格式、配置项的改动是否向后兼容；不兼容的话调用方有没有一起改。
3. **安全**：外部输入是否当成数据处理；有没有把凭据写进日志或输出；权限检查有没有被绕开。
4. **测试**：新行为有没有测试；测试在改动之前会不会失败（不会失败的测试证明不了任何事）。
5. **可读性**：命名、注释是否解释了「为什么」；只在前四项都没问题时再提。

每条意见写清：在哪（文件:行）、会怎样出错（具体输入 → 错误结果）、建议怎么改。
拿不准的标成疑问，不要写成结论。没发现问题就直接说没发现，不要为了有输出而凑意见。
`,
  ),
  official(
    'debugging',
    '排查一个 bug：先复现，再缩小范围，最后才改代码',
    ['fs.read', 'shell.exec'],
    `
# 排查问题

1. **复现**：拿到能稳定触发问题的最小步骤（命令、输入、环境）。复现不了就先问用户要信息，不要猜着改。
2. **读错误本身**：完整的报错与堆栈，最早出现的那一条通常比最后一条更接近原因。
3. **缩小范围**：二分——注释掉一半、换一个输入、看 \`git log\` 最近动过什么（必要时 \`git bisect\`）。每一步只改一个变量。
4. **写下假设再验证**：「我认为是 X，因为 Y；如果是 X，那么做 Z 会看到 W」。验证不了的假设不当结论。
5. **修复**：先写一个会失败的测试，再改代码让它通过；改动只针对原因，不顺手重构。
6. **收尾**：说明根因、修法、还有哪些地方可能有同类问题。
`,
  ),
]
