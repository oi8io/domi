# @domi/protocol 事件契约快照

> 由 `bun run scripts/gen-api-snapshot.ts` 生成，**不要手改**。
> 这份文件变了就意味着协议契约变了（INV-01）。
> 改之前先回答一个问题：**用旧版本写下的事件，新代码还能不能解析？**

SCHEMA_VERSION = 11

## 事件类型

budget.decided
  action: "continue" | "stop" | "raise"
  kind: "tokens" | "costUsd" | "toolCalls"
  limit: number?

budget.warn
  kind: "tokens" | "costUsd" | "toolCalls"
  limit: number
  used: number

ctx.cleanup
  fromSeq: number
  preserved: number[]
  saved: object
  tokensAfter: number
  tokensBefore: number
  toSeq: number

ctx.compact
  fromSeq: number
  keptTurns: number
  summary: object
  tokensAfter: number
  tokensBefore: number
  toSeq: number
  trigger: "threshold" | "manual"

ctx.ref
  fromSeq: number
  sessionId: string
  toSeq: number

error
  counters: object?
  message: string
  recoverable: boolean
  scope: string

fs.snapshot
  bytes: number
  path: string
  phase: "before" | "after"
  sha256: string | null

hook.run
  blocked: boolean
  capabilityId: string?
  exitCode: number | null
  ms: number
  name: string
  on: "pre" | "post" | "stop"
  output: string?
  timedOut: boolean

memory.write
  changes: object[]?
  diff: string
  item: object?
  itemId: string?
  layer: "L3" | "L4"
  op: "add" | "delete" | "extracted" | "update" | "review"
  range: object?
  reviews: object[]?

mode.switch
  reason: string?
  to: "plan" | "act"

model.delta
  text: string

model.reason
  text: string

model.request
  model: string
  provider: string
  tokensIn: number

model.switch
  from: string
  lostCapabilities: string[]?
  reason: string?
  to: string

model.usage
  raw: record<string, unknown>

permission
  capabilityId: string
  channel: string?
  decision: "allow" | "deny" | "ask"
  grant: object?
  matchedRule: string | null
  source: "default" | "config" | "user" | "mode" | "session-grant"

plan.decided
  approved: boolean
  asTask: boolean?
  comment: string?
  runId: string?

plan.proposed
  plan: string
  steps: object[]?

plugin.error
  message: string
  plugin: string
  tool: string?

project.assign
  auto: boolean
  path: string
  projectId: string

revert
  scope: "files" | "conversation" | "both"
  snapshotId: string | null
  toSeq: number
  undoSnapshotId: string | null

review.findings
  findings: object[]

schedule.fire
  due: number
  late: boolean
  scheduleId: string

session.kind
  cwd: string
  isolation: object?
  kind: "chat" | "task"

snapshot
  files: number
  id: string
  label: string
  largeFilesSkipped: number?

task.end
  status: "done" | "failed" | "cancelled"

task.node
  attempt: number
  error: string?
  ms: number?
  nodeId: string
  output: string?
  sessionId: string?
  status: "started" | "done" | "failed"

task.resume
  completed: string[]
  rerun: string[]

task.retry
  nodeId: string

task.run
  cwd: string?
  name: string
  spec: unknown

task.spawn
  childSessionId: string
  goal: string
  tools: string[]?

tool.call
  args: unknown
  id: string
  name: string

tool.result
  id: string
  ms: number
  ok: boolean
  payload: unknown
  reason: string?

user.input
  attachments: object[]?
  text: string

verify.required
  attempt: number
  final: boolean?
  message: string

workspace.trust
  root: string
  source: "user" | "stored" | "default"
  trusted: boolean

worktree.apply
  commit: string?
  message: string?
  mode: "squash" | "merge" | "branch"
  ok: boolean

worktree.create
  base: string
  branch: string
  path: string
  repo: string

worktree.discard
  path: string
  trash: string

worktree.restore
  path: string
  trash: string

## 信封

  ev: union
  parentSeq: number | null
  schemaVersion: number
  seq: number
  sessionId: string
  ts: number

## 导出符号

  AUTH_SUBPROTOCOL
  DomiEventSchema
  ErrorCode
  EventEnvelopeSchema
  METHODS
  METHOD_NAMES
  MessageSchema
  ModelMessageSchema
  NOTIFICATIONS
  NOTIFICATION_NAMES
  NotificationSchema
  PROTOCOL_VERSION
  ProjectSchema
  ProjectSettingsSchema
  RefLinkSchema
  RefSchema
  RequestSchema
  ResponseSchema
  RpcErrorSchema
  SCHEMA_VERSION
  SOUL_SECTIONS
  SemanticItemSchema
  SoulChangeSchema
  TOKEN_CHARS
  TOKEN_SUBPROTOCOL_PREFIX
  ToolCallSchema
  ToolSchemaSchema
  UnknownEventSchema
  authProtocols
  fail
  isKnownEvent
  isNotification
  isRequest
  isResponse
  isUnknownEvent
  notify
  ok
  parseEvent
  versionMismatch
