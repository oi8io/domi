# @domi/plugin 插件 API 快照

> 由 `bun run scripts/gen-api-snapshot.ts` 生成，**不要手改**。
> 这份文件变了就意味着插件 API 变了（PRD-M6-001 AC-4，docs/adr/022）：
> 只加可选字段 = 小版本；改名、删字段、改含义 = 主版本，同时要登记弃用。

PLUGIN_API_VERSIONS = [1]
MANIFEST_FILE = domi-plugin.yaml
HOST_CALLS = readFile, writeFile, fetch, log

## manifest

  api: number
  author: string?
  contributes: default
    mcp: default
      args: default
      command: string?
      cwd: string?
      enabled: default
      env: default
      headers: record<string, string>?
      name: string
      timeoutMs: number?
      url: string?
    skills: default
    tools: default
      description: string
      entry: string
      input: default
      name: string
      timeoutMs: default
    ui: default
      entry: string
      id: string
      title: string
  description: default
  homepage: string?
  name: string
  permissions: object
    hosts: default
    read: default
    write: default
  version: string

## 弃用

  contributes.tools[].main（1.0 起弃用，2.0 移除；改用 contributes.tools[].entry）

## 导出符号

  DEPRECATIONS
  HOST_CALLS
  InstallRefusedError
  MANIFEST_FILE
  ManifestError
  ManifestSchema
  PLUGIN_API_VERSION
  PLUGIN_API_VERSIONS
  PLUGIN_MANIFEST_RULE
  PluginHost
  PluginPermissionsSchema
  PluginToolError
  PluginToolSchema
  PluginUiSchema
  bunExecutable
  darwinProfile
  definePlugin
  describePermissions
  detectSandbox
  ensureRunner
  indexPath
  insidePaths
  installPlugin
  loadInstalled
  parseManifest
  pathAllowed
  readIndex
  readManifestFile
  removePlugin
  runSandboxed
  sandboxCommand
  scaffold
  scaffoldFiles
  sha256
  toolId
