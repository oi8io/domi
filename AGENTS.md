# AGENTS.md — AI 开工必读

任何 AI 会话在写第一行 domi 代码之前，**必须先读完本文件指向的三份文件**。
跳过这一步是"合理偏离"最主要的发生场景。

## 每次开工的强制加载顺序

1. `PRD-VISION.md` —— 12 条不变量。**这是唯一的跑偏判定依据。**
2. `docs/spec/M{当前}.md` —— 当前里程碑的接口契约与取舍记录
3. `docs/tasks/M{当前}.md` —— 找到 `status: doing` 的任务，读它的"笔记"字段
4. `NEXT.md` —— 三行现状

## 硬规则

- **回写门**：发现上游文档（PRD/SPEC）有问题，**停下，回去改文档，再往下走**。
  严禁"心里知道文档错了，代码按正确的写"。回写 commit 前缀 `docs(writeback):`。
- **没有 PRD 引用的任务不允许存在**（INV-10）。要做的事不在 PRD 里？先回去改 PRD。
- **测试先行**：先提交失败的测试，再提交实现。任务文件里测试条目排在实现条目之前。
- **任务粒度 ≤ 0.5 天**，结束时 main 必须可跑（INV-07）。
- **commit message 带 task id**：`feat(kernel): 权限引擎默认拒绝 [TASK-M0-007]`
- **收工前更新 `NEXT.md` 和当前任务的"笔记"字段。** 这是纪律，不是可选项。
- **spike 代码必须删除重写**，不允许"跑通了顺手留下"。

## 角色切换

本项目按 PM → 架构师 → 工程师 → QA 四个环节推进，见 `PROCESS.md`。
当前处在哪一环，看 `NEXT.md`。**不要跨环节工作**——写代码时不要顺手改 PRD，改 PRD 要走回写门。

## QA 环节的特殊要求

QA 必须在**独立会话**中进行，只加载 `PRD-VISION.md` + `docs/prd/` + 代码，
**不加载实现过程的对话历史**。目标是证伪，不是确认。
